- [2026-07-26] TSK-001 — Esqueleto del monorepo (apps/web, apps/engine) + Bun instalado. must. claude-code. 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda). Esto significa que ahora dota2coach tiene los dos procesos base corriendo: el sitio (apps/web, Next.js) y el motor (apps/engine, Bun) respondiendo su primer chequeo de salud real en 127.0.0.1 — la base sobre la que se construye todo lo demás (esquema de datos, captura de draft, motor de sugerencias).
- [2026-07-27] TSK-002 — Esquema SQLite/Drizzle (C4). must. codex → claude-code (Handoff, sin créditos en Codex). 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda; hallazgo del .gitignore corregido en la misma ronda). Esto significa que ahora dota2coach puede guardar localmente los datos de héroes, winrates por parche y enfrentamientos entre héroes que vienen de OpenDota — la base de datos sobre la que se van a calcular las señales de sugerencia (contrapick, meta del parche). De regalo: el script que verifica los límites del proyecto (verify-simplicity.sh) ahora mide de verdad — antes ignoraba el código nuevo y penalizaba el registro de actividad de rutina.
- [2026-07-27] TSK-003 — OpenDotaClient + sincronización de meta (S6). must. codex → claude-code (Handoff, sin tokens en Codex). 1 intento, 2 rondas de @redteam (la primera con REJECTED real por seguridad: una URL de imagen se construía a partir de datos de OpenDota sin validar su forma, abriendo la puerta a que apuntara a un servidor que no es el de Valve — corregido y reverificado por ejecución antes de la segunda ronda). Esto significa que ahora dota2coach puede traer datos reales de OpenDota (héroes, winrates por parche, enfrentamientos) y guardarlos en la base local, con reintentos automáticos si OpenDota está ocupado o caído — sin que un dato mal formado o malicioso llegue a corromper el cache. Es la primera vez que el proyecto valida input externo de verdad, no solo de nombre.
- [2026-07-27] TSK-004 — applyDraftEvent puro + contrato de eventos (S1, S4). must. codex → claude-code (assigned directo, sin ciclo de Handoff, por el patrón confirmado 2/2 veces en TSK-002/TSK-003 de falta de créditos en Codex). 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda, sin hallazgos bloqueantes). Esto significa que ahora dota2coach tiene el corazón del seguimiento de un draft en vivo: una función pura que toma el estado actual y un evento capturado (pick, ban, cambio de fase, corrección) y devuelve el nuevo estado o un motivo de rechazo explícito — nunca se cae, nunca corrompe lo que ya se sabía del draft, aunque lleguen eventos duplicados, fuera de orden, o de un formato de draft aún desconocido. Es la pieza sobre la que se va a construir el motor de sugerencias (C3) y el servidor Bun (ingesta + WebSocket). Queda documentada una nota abierta: la validación real de "este HeroId existe de verdad" contra el catálogo de héroes todavía no tiene dueño — se decide en un ticket futuro (ingesta S1 o el paso de candidatos de C3).
- [2026-07-27] TSK-005 — SignalScorer: counter (contrapick, S3). must. codex → claude-code (assigned directo, mismo criterio de TSK-004: sin señal de créditos restaurados en Codex). 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda, sin hallazgos bloqueantes). Esto significa que ahora dota2coach tiene la primera de las cuatro señales que van a alimentar las sugerencias de pick: dado un candidato y los héroes enemigos ya elegidos, calcula si históricamente ese héroe rinde mejor o peor de lo normal contra ellos (contrapick real, con datos de OpenDota, nunca inventado) y explica el resultado en una frase concreta ("Fuerte contra Lina y Zeus") en vez de un número sin contexto. Si no hay suficiente dato (enemigos desconocidos, o enfrentamientos con muestra chica), la señal dice honestamente "no sé" en vez de fingir neutralidad. Quedan documentadas 3 notas abiertas para cuando se construyan las otras señales (TSK-006/007/008) y la mezcla final (TSK-009): la forma de `MetaSnapshot` va a necesitar crecer, y `weighted` todavía no está calculado de verdad — eso lo resuelve TSK-009.
- [2026-07-27] TSK-006 — SignalScorer: patch_meta (S3). must. codex → claude-code (assigned directo, mismo criterio de TSK-004/005). 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda, sin hallazgos bloqueantes). Esto significa que ahora dota2coach tiene la segunda de las cuatro señales de sugerencia: qué tan bien rinde un héroe candidato EN ESTE PARCHE, mirando solo partidas de jugadores de nivel bajo/medio (el público real del producto), nunca datos de pro ni el promedio de todos los niveles mezclados — evita recomendar picks que solo funcionan en manos de un profesional. Si no hay suficiente muestra (menos de 500 partidas en ese rango), la señal dice honestamente "no sé" en vez de inventar un número. Queda documentada la definición de "bracket bajo/medio" (sin estándar oficial de Valve) como punto único a revisar si el producto define después su propia taxonomía.
- [2026-07-27] TSK-007 — SignalScorer: team_synergy (S3). must. codex → claude-code (assigned directo, mismo criterio de TSK-004/005/006). 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda, sin hallazgos bloqueantes). Esto significa que ahora dota2coach tiene la tercera de las cuatro señales de sugerencia: en vez de mirar datos históricos, mira lo que el equipo propio YA tiene y lo que le falta (control, iniciación, aguante, empuje, soporte) y premia al candidato que llena un hueco real, no al que repite una capacidad que el equipo ya cubrió. Es explícitamente una heurística (no hay dato medido de sinergia real entre héroes sin pagar por STRATZ), así que siempre se marca con sampleSize 0 para que la UI la muestre como estimada, no medida. Queda documentado el mapeo de las 5 capacidades a los roles reales de OpenDota como el punto a revisar si el producto quiere afinar la taxonomía más adelante.
- [2026-07-27] TSK-008 — SignalScorer: role_gap (S3). must. codex → claude-code (assigned directo, mismo criterio de TSK-004/005/006/007). 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda, sin hallazgos bloqueantes). Esto significa que ahora dota2coach tiene la cuarta y última señal de fase 1: penaliza a un candidato carry cuando el equipo propio ya tiene dos héroes compitiendo por el mismo farm, sin duplicar la lógica de team_synergy (esa mira capacidades, esta mira recursos) — verificado con una prueba dedicada que demuestra la independencia real entre ambas señales, no solo declarada. Con esto, las 4 señales del motor de sugerencias (counter, patch_meta, team_synergy, role_gap) están completas y probadas de forma aislada. Queda pendiente TSK-009: la mezcla que las combina con SCORING_WEIGHTS_V1, normaliza a 0-100 y redistribuye el peso cuando alguna señal da null — ese es el siguiente hito real del motor.
- [2026-07-27] TSK-009 — Mezcla, orden y explicación del motor (§C3). must. claude-code (asignado desde la creación del ticket, no codex -- el propio autor lo marcó como el riesgo central del proyecto). 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda reforzada, 11 hipótesis de bug rastreadas). Esto significa que ahora dota2coach puede producir una sugerencia real de punta a punta: toma el estado del draft, calcula las 4 señales para cada héroe disponible, las combina con pesos versionados (SCORING_WEIGHTS_V1) redistribuyendo el peso cuando alguna señal no tiene dato suficiente (nunca la trata como neutral), ordena el top 3, y arma una explicación en español trazable a las señales reales que la produjeron — con una marca honesta de confianza (alta/media/baja) que nunca esconde una sugerencia débil. Con esto, el motor de sugerencias (C3) queda funcionalmente completo por primera vez. Quedan documentadas 4 notas abiertas (calibración de rangos de normalización, camino de excepción sin probar, semántica del flag "sin candidatos", cobertura de menos de 3 candidatos) para revisión futura, ninguna bloqueante.
- [2026-07-27] TSK-015 — SqliteMetaProvider: puente SQLite -> MetaSnapshot (S2/C4). must. claude-code. 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda, sin hallazgos bloqueantes). Ticket creado a mitad de sesión al descubrir, durante la planificación de TSK-010, que TSK-002 había dejado explícitamente pendiente "un ticket separado" para esto que nunca se creó — sin él, el motor de sugerencias (TSK-009, ya listo) no tenía forma de leer datos reales de la base local. Esto significa que ahora dota2coach puede armar, con 3 consultas (nunca una por héroe, así el motor nunca espera de más al calcular sugerencias durante un draft), el snapshot completo de meta que el motor necesita, y sabe decir honestamente si ese cache tiene más de 24 horas de antigüedad. Con esto, TSK-010 queda desbloqueada para retomarse.
- [2026-07-27] TSK-011 — Simulador de draft + guiones de prueba. must. codex → claude-code (assigned directo, mismo criterio de TSK-004 en adelante). 1 intento. @redteam: APPROVED (gate seguridad PASS, 1 ronda, evaluado sobre los 3 archivos propios por una sesión paralela de TSK-010 corriendo sobre el mismo repo). Esto significa que ahora dota2coach puede reproducir un draft completo (Captain Mode o All Pick) sin tener Dota 2 abierto ni un cliente real conectado -- útil para probar el reductor de estado, el motor de sugerencias y, más adelante, la vista en vivo, con datos deterministas y reproducibles. Incluye la corrección de un falso positivo de captura (pick_reverted) en ambos guiones, y emite por la misma ruta de ingesta (POST /ingest/draft-event) que un capturador real -- nunca un atajo. Queda documentada una nota no bloqueante: `runSimulator` no valida `speed > 0` todavía (no explotable hoy, no hay ningún punto de entrada externo que lo alimente); revisar cuando exista un CLI o endpoint que exponga el simulador.
- [2026-07-27] TSK-010 — Servidor Bun: ingreso HTTP + WebSocket + seguridad (§5). must. claude-code (asignado desde la creación del ticket -- sensible a seguridad, no codex). 1 intento, 2 rondas de @redteam (ronda 1 REJECTED real por seguridad: un mensaje WebSocket dejaba pasar un sessionId sin validar su tipo hacia el estado real del draft — corregido y reverificado por ejecución antes de la ronda 2). Esto significa que ahora dota2coach tiene un servidor real corriendo en la máquina del jugador: recibe eventos de draft del capturador (con token de autenticación generado en cada arranque, nunca guardado en el repo), los aplica al reductor, calcula sugerencias con datos reales de la base local, y empuja todo por WebSocket a la vista en vivo en el orden correcto (estado del draft primero, sugerencias después) — con límite de 20 eventos por segundo para que un capturador con errores no pueda saturarlo. En el camino se encontraron y corrigieron dos problemas reales fuera del alcance original del ticket: el servidor nunca arrancaba en una instalación nueva (directorio de datos faltante), y el propio gate de seguridad del proyecto tenía un punto ciego para secretos en archivos nuevos desde el primer commit — ambos corregidos con el visto bueno explícito del usuario. Con esto, dota2coach tiene por primera vez un camino de punta a punta real: captura → reductor → motor → WebSocket.
- [2026-07-27] TSK-012 — Vista de draft en Next.js (S5) con sus 6 estados. must. claude-code. 1 intento, 2 rondas de @redteam (ronda 1 REJECTED real por seguridad: mensajes de WebSocket confiados sin validar su forma en runtime, combinado con la ausencia total de un error boundary en apps/web -- riesgo real de pantalla en blanco durante un draft en vivo, corregido y reverificado por ejecución antes de la ronda 2). Esto significa que ahora dota2coach tiene, por primera vez, una interfaz real donde un jugador puede ver su draft en vivo: los 6 estados obligatorios (desconectado, esperando el draft, activo, degradado, completo, error) siempre existen en pantalla, nunca se calla una sugerencia de confianza baja ni una señal sin dato, y los íconos de héroe solo se muestran si vienen del CDN oficial de Valve. Es el mismo componente tanto en una pestaña normal del navegador como preparado para un overlay de Overwolf, sin ninguna rama de código distinta entre ambos casos. En el camino se instaló Zustand (única excepción de datos para el draft en vivo, vía /gear-up + @depcheck) y se corrigió un tercer punto ciego real en el gate de simplicidad del proyecto: la sección de dependencias nunca funcionó en este monorepo desde que existe (apuntaba a un package.json de raíz inexistente). Con TSK-012, dota2coach tiene por primera vez un camino de punta a punta visible para el usuario: motor -> servidor -> vista en vivo.
- [2026-07-27] TSK-013 — Entrada manual y camino de degradación. must. claude-code. 1 intento, 1 ronda de @redteam (APPROVED; un hallazgo real de manejo de fallos de red -- precisamente el escenario para el que esta feature existe -- encontrado y corregido dentro de la misma revisión, sin necesidad de una ronda formal de rechazo). Esto significa que ahora dota2coach tiene su segundo capturador de primera clase completo: cuando la detección automática falla o no existe, el jugador puede marcar picks y bans a mano desde la misma vista, con un buscador que nunca deja inventar un héroe que no existe en el catálogo real. Cuando la captura automática se pierde a mitad de un draft, la interfaz avisa y habilita la entrada manual sin perder nada de lo ya capturado. Y cuando cualquier capturador reporta un pick con poca confianza, ahora queda marcado como "sin confirmar" en el propio motor (un gap real de la especificación que el ticket original del reductor había dejado sin cubrir, corregido aquí) con un botón para corregirlo sin perder el resto del draft. Verificado de punta a punta contra el servidor real, no solo con pruebas unitarias.
- [2026-07-27] TSK-014 — Páginas del sitio (estado del meta, héroes, configuración) con RTK Query. should. claude-code. 1 intento, 1 ronda de @redteam (APPROVED; un hallazgo GRAVE encontrado y corregido durante la revisión: sin CORS, ninguna de las 3 páginas habría funcionado desde un navegador real pese a que todas las pruebas de servidor pasaran limpio). Esto significa que ahora dota2coach tiene sus primeras páginas de sitio normales, separadas de la vista de draft en vivo: dónde ver si el meta está actualizado y disparar una sincronización manual con un clic, la lista completa de héroes con su ícono oficial, y un lugar simple para guardar preferencias locales. En el camino se corrigieron dos bugs reales fuera del alcance original: el script de migraciones tenía el mismo problema que ya se había arreglado en otro archivo (TSK-010) pero nunca se propagó, y un patrón de React ya no funciona en la versión de este proyecto (advertido por AGENTS.md, confirmado en la práctica). Con esto, las 4 páginas normales + la vista de draft en vivo completan la superficie visible de fase 1 de dota2coach.
- [2026-07-28] TSK-016 — Ambiente de pruebas comprensible -- simulador visible, configuración previa y guía de estado. must. claude-code. 1 intento, 1 ronda de @redteam (APPROVED; 4 hallazgos reales de manejo de errores/condición de carrera encontrados y corregidos dentro de la misma revisión, sin ronda formal de rechazo -- mismo patrón que TSK-013). Esto significa que ahora dota2coach tiene, por primera vez, un ambiente de pruebas que un humano sin contexto previo puede entender: el botón "Simular draft" (antes un no-op sin conectar desde TSK-012) abre una pantalla real donde elegir el escenario (Captain Mode/All Pick) y el modo de reproducción -- a ritmo visible con velocidad configurable, o paso a paso con un botón "Siguiente pick/ban" -- en vez de que todo el draft apareciera de golpe ya en "Draft final". Los 6 estados de la vista de draft (TSK-012) ahora explican en lenguaje llano qué son y qué puede hacer el usuario, la entrada manual deja de quedar abierta sin explicación una vez el draft termina, y el indicador de qué lado es del usuario pasó de un texto pequeño a una etiqueta reconocible de un vistazo. Cero endpoint nuevo en el motor: todo reutiliza POST /api/session/manual ya existente (TSK-013); el único cambio en apps/engine fue agregar un modo paso a paso a player.ts sin tocar su lógica de reproducción ya probada. Verificado con un smoke test real de punta a punta contra el motor vivo (no solo pruebas unitarias), confirmando el orden draft_state/suggestions y la transición active->complete. Nota abierta: sin verificación visual en un navegador real dentro de esta sesión (sin herramienta de automatización de navegador disponible) -- queda pendiente que el usuario lo confirme a ojo.
- [2026-07-29] TSK-028 — Simulador: el lado del usuario saca héroes de su pool configurado. must. claude-code. 1 intento, 1 ronda de @redteam (APPROVED; un hallazgo real -- el evento de revertir un pick no seguía al héroe realmente sustituido -- encontrado y corregido dentro de la misma revisión, sin ronda formal de rechazo, mismo patrón que TSK-013/016). Esto significa que ahora el ambiente de pruebas del simulador tiene sentido con tu pool configurado: cuando le toca pickear a tu lado, en vez de un héroe al azar del guion, sale el siguiente héroe de tu pool guardado -- y si tu pool se agota antes de que termine el draft, el resto sigue el guion original sin romperse. El lado rival nunca se toca, ningún héroe se repite ni siquiera cuando el guion revierte un pick a mitad de camino, y si todavía no configuraste un pool, el simulador se comporta exactamente igual que hasta ahora. Responde directo al reporte de producto: "¿para qué guardás tu pool si el ambiente de pruebas no lo usa?".
- [2026-07-29] TSK-029 — Pantalla de inicio real + navegación compartida entre pantallas. must. claude-code. 1 intento, 1 ronda de @redteam (APPROVED; un hallazgo real de accesibilidad -- el nav no mostraba foco visible al navegar con teclado -- encontrado y corregido dentro de la misma revisión). Esto significa que ahora dota2coach deja de sentirse como un conjunto de pantallas sueltas: la primera vez que alguien abre el sitio ve una landing real (no el texto de ejemplo de Next.js) con los 3 caminos principales, y desde cualquier pantalla -- draft en vivo, tu pool de héroes, catálogo de héroes, estado del meta, configuración -- se puede llegar a cualquiera de las otras sin escribir la URL a mano ni perderse. Y cuando guardás tu pool de héroes, ya no queda la pregunta de "¿y ahora qué?" -- aparece un camino directo hacia el draft en vivo, donde ese pool empieza a importar de verdad (incluido el simulador, TSK-028). Responde directo al reporte de producto: "no tenemos un orden de sitemap o de pantallas o de flujos... todo está independiente".
- [2026-07-29] TSK-030 — Persistir account_id en /hero-pool vía /api/settings. should. codex → claude-code (mismo criterio que TSK-024/026: cambio acotado pero se apoya en un patrón de estado ya establecido en el mismo componente y en la regla dura de dato personal, mejor con el contexto ya cargado). 1 intento, 1 ronda de @redteam (APPROVED, foco específico en que el account_id nunca se loguea ni se eco -- verificado línea por línea, no solo por ausencia de console.log). Esto significa que ahora, si guardaste tu cuenta de Steam para calcular tu pool de héroes, no tenés que volver a escribirla cada vez que recargás la página -- queda pre-poblada, igual que cualquier otra preferencia del sitio. Se guarda recién cuando el cálculo funciona de verdad (nunca un valor que el servidor iba a rechazar), y si esa escritura de conveniencia falla por cualquier motivo, nunca tapa un cálculo que sí salió bien.
- [2026-07-29] TSK-031 — Ampliar el guion de bans de allPick en scripts.json. should. codex → claude-code (mismo criterio del bloque: evita repetir el patrón de créditos agotados ya confirmado antes en el proyecto). 1 intento, 1 ronda de @redteam (APPROVED, sin hallazgos). Esto significa que el escenario "All Pick" del ambiente de pruebas ahora se siente como un draft real de Dota 2: pasa de 2 baneos a 16, la cantidad que de verdad se ve en una partida pública del parche 7.35d, en vez de una prueba de humo mínima que nunca se pensó para sentirse real. Con este ticket se cierra el bloque completo de 5 tickets (TSK-027 a TSK-031) abierto tras la primera vez que el usuario probó fase 1b de punta a punta y reportó los 4 problemas concretos del simulador y de la falta de navegación entre pantallas.
- [2026-08-01] TSK-032 — Comparación explícita entre las 2 mejores sugerencias. should. claude-code. 1 intento, 1 ronda de @redteam (APPROVED; un hallazgo real de exactitud en un comentario -- afirmaba una garantía matemática que no siempre se cumple -- corregido; el código en sí ya era correcto). Esto significa que ahora, cuando el motor te sugiere un héroe por encima de otro, no solo ves las señales de cada uno por separado -- ves directamente por qué el primero le gana al segundo ("le gana a X principalmente por contrapick", por ejemplo). La comparación nunca inventa una ventaja: solo mira señales donde ambos candidatos tienen dato real, y si no hay ninguna señal comparable que favorezca claramente al primero (empate exacto, o la ventaja real está en una señal que el otro no tiene), simplemente no se muestra nada en vez de fabricar una explicación. Con este ticket se cierra por completo el bloque de 6 tickets (TSK-027 a TSK-032) abierto tras la primera vez que el usuario probó fase 1b de punta a punta.
- [2026-08-01] TSK-033 — Selector de ventana de días en "Calcular desde mis partidas". must. claude-code. 1 intento, 1 ronda de @redteam (APPROVED, sin hallazgos). Esto significa que cuando el sistema te dice "ningún héroe pasó el mínimo de partidas -- probá ampliar la ventana", ahora existe un control real para hacer justamente eso, en vez de una promesa que la pantalla no dejaba cumplir. Encontrado y arreglado en vivo durante una sesión de QA manual del usuario -- probado con cuentas de Steam reales, confirmando además que el motivo por el que algunas cuentas nunca traen datos (sin importar la ventana) es la configuración de privacidad de esa cuenta en Steam, no un bug del sistema.
