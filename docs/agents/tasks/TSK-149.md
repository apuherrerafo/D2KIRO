---
id: TSK-149
title: "Fase 7 G1: scripts/pro/ingest-tournaments.ts — catálogo de torneos"
state: backlog
moscow: must
attempts: 0
preferred_tool: claude-code
created_at: 2026-08-27
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 7 (`SPEC.md` §14.4-A), **Gate 1, ticket 4**.
Depende de `TSK-148`.

Script manual, corre en la máquina del desarrollador — **nunca programado, nunca en CI, nunca
invocado desde `apps/engine`**. Mismo estatus que `corpus:sync` y que el regenerador de
`hero-positions.json`.

## El problema real que resuelve
`/leagues` devuelve 10.117 ligas con `leagueid`, `tier` y `name`, y **ningún campo de fecha**
(verificado; ya lo había documentado `evt-20260824-111`). Sin fecha no se puede saber qué torneos
son recientes ni acotar la ingesta de drafts. La fecha se **deriva** cruzando con `/proMatches`,
que sí trae `start_time` por partida y su `leagueid`.

## Alcance — 1 archivo
**Nuevo** `scripts/pro/ingest-tournaments.ts` (+ su `.test.ts`).

1. `/leagues` → tier y nombre por `leagueid`.
2. Paginar `/proMatches` hacia atrás (cursor `less_than_match_id`, 100 por página) acumulando por
   `leagueid` el `start_time` mínimo y máximo vistos → `firstSeenAt` / `lastSeenAt`.
3. Escribir `tournaments` con `ProTournament` completo: `source`, `fetchedAt`, `confidence`.

Reutiliza `OpenDotaClient` (`apps/engine/src/meta/opendota-client.ts`) — **no reimplementa el
fetch**. Su retry 1s/4s/16s ya está probado y es contrato de `apps/engine`: no se toca.

## Reglas duras
- **`firstSeenAt`/`lastSeenAt` son derivados, no la fecha oficial del torneo.** Se emiten con
  `confidence: "medium"` y el documento de `TSK-147` ya explica por qué. Nunca se presentan como
  la fecha real del evento.
- **`region` es siempre `"unknown"`.** Verificado: `region` viene `null` en LAN (TI 2026). Prohibido
  inferirla del nombre de la liga.
- El `tier` se copia tal cual de OpenDota (`premium`/`professional`/`excluded`/`amateur`/`null` →
  `"unknown"`). **Nunca se traduce a "Tier 1" en el dato** — esa equivalencia vive en `TSK-152` y
  en documentación.
- Un `leagueid` visto en `/proMatches` pero ausente de `/leagues` se registra con
  `tier: "unknown"`, **nunca se descarta en silencio**.

## Criterios de aceptación
- Re-correr el script sobre datos ya ingeridos escribe **0 filas nuevas** (idempotencia real,
  verificada contando filas antes y después).
- Prueba con fixture grabado de `/leagues` + `/proMatches`: **cero red en las pruebas**, mismo
  criterio que S6/S7.
- Una liga sin partidas en la ventana paginada queda con `firstSeenAt`/`lastSeenAt` nulos y
  `confidence: "none"` — no se inventa una fecha.
- El recuento por tier reproduce los órdenes de magnitud documentados en `TSK-147`
  (premium ~214, professional ~2.475).

## Fuera de alcance
Ingerir drafts (`TSK-150`). Clasificar Tier 1 (`TSK-152`) — este ticket solo guarda el `tier` crudo.
