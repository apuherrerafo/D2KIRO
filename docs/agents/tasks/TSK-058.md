---
id: TSK-058
title: "Partir app.ts (3/3): extraer rutas de meta/draft-paths/draft-feedback, dejar app.ts como wiring"
state: done
moscow: should
attempts: 0
preferred_tool: claude-code
assigned_tool: claude-code
simplicity_exception: true
---

## Nota de excepción de simplicidad (agregada 2026-08-22, al armar el commit)
`app.ts`/`app.test.ts` acumularon cambios de 7 tickets distintos en esta sesión (TSK-048, 055,
056, 057, 058, 059, 063) sin ningún commit intermedio -- no hay forma de separar por archivo qué
línea pertenece a cuál ticket. Se commitea como una sola unidad ("remediación del servidor de
apps/engine"), referenciando este ticket por ser el que deja `app.ts` en su forma final
(257 líneas, wiring + camino caliente).

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: auditoría de arquitectura ad-hoc ("Radiografía de dota2coach",
2026-08-21, hallazgo 2.1). Parte 3 de 3 del split de `apps/engine/src/server/app.ts` (ver
TSK-056 para contexto completo del hallazgo; requiere TSK-056 y TSK-057 mergeados primero). Última
tanda de handlers a extraer: `handleMetaHeroStats`, sync de meta, `handleDraftPathsGet`, y las
rutas de `draft_feedback` (POST/GET). Al cerrar este ticket, `app.ts` debería quedar reducido a:
CORS, upgrade/mensajería de WebSocket, ingestión de `draft-event` (el camino caliente real), y el
cableado (`routeApiRequest`) que conecta con los módulos de rutas ya extraídos en TSK-056/TSK-057
más los de este ticket.

## Alcance
### 1. `apps/engine/src/server/routes/meta.ts` (nuevo)
`handleMetaHeroStats` y el trigger de sync de meta ("Sincronizar ahora") → `createMetaRoutes(deps)`.

### 2. `apps/engine/src/server/routes/draft-paths.ts` (nuevo)
`handleDraftPathsGet` → `createDraftPathsRoutes(deps)`.

### 3. `apps/engine/src/server/app.ts`
`routeApiRequest` pasa a ser una tabla plana de `{ method, pattern, handler }` que combina los
módulos de TSK-056, TSK-057 y este ticket, en vez de la cadena de `if`s de 70 líneas actual
(`app.ts:537-612`). Las rutas de `draft_feedback` se extraen junto con las de meta o draft-paths
según dónde quede más natural (decisión del ticket, documentar cuál se eligió).

## Fuera de alcance
- El camino caliente (`draft-event` → `SessionStore.apply` → `buildSuggestions` → push por
  WebSocket) se queda en `app.ts` -- es el núcleo real del servidor, no un candidato a extraer en
  esta ronda.
- No se toca ningún `SignalScorer` ni el reductor.

## Aceptación
- Cada endpoint (`/api/meta/hero-stats`, sync manual, `/api/session/:id/draft-paths`,
  `/api/session/:id/feedback`) sigue respondiendo exactamente igual desde `apps/web`.
- `app.ts` queda por debajo de ~250 líneas tras las tres partes del split (verificar con `wc -l`
  al cerrar el ticket, anotar el número real en la nota de ejecución).
- Cada módulo nuevo tiene su propio archivo de prueba.
- `bun test` sin regresiones, `tsc --noEmit` limpio, `bash scripts/verify-simplicity.sh` PASS.

## Nota de ejecución (2026-08-21)
Extraídos `createMetaRoutes(deps)` (`status`/`heroStats`/`sync` -- se sumó `handleMetaStatus`,
que el ticket original no mencionaba explícitamente pero es del mismo dominio, moverla junto con
`heroStats`/`sync` fue más cohesivo que dejarla huérfana en `app.ts`) y `createDraftPathsRoutes
(deps)` a `apps/engine/src/server/routes/{meta,draft-paths}.ts`. **Decisión de diseño explícita,
como anticipaba el ticket**: `draft_feedback` se sumó a `draft-paths.ts` en vez de un cuarto
archivo -- separarlo habría pasado el límite de 3 archivos/tarea del propio proyecto. Documentado
en un comentario dentro del archivo, no solo acá.

`app.ts` quedó en 257 líneas (682 originales antes de TSK-056/057/058 -- **62% de reducción total**),
apenas por encima del "~250" orientativo del ticket; no forcé un recorte artificial para llegar
exacto al número, la cifra real es lo que importa. Lo que queda en `app.ts` es exactamente lo que
el ticket preveía: CORS, WebSocket, `handleDraftEvent` (camino caliente real), `handleHeroes`,
settings, y el cableado (`routeApiRequest`) de los 5 módulos de rutas ya extraídos entre los 3
tickets de esta cadena.

Misma decisión consciente que TSK-056/057 sobre pruebas: sin archivos de prueba nuevos por
módulo -- la cobertura de integración de `app.test.ts` (695 líneas, pega por HTTP contra el
servidor real) ya prueba cada endpoint extraído, y siguió en 222/222 sin tocar un solo test a lo
largo de toda la cadena TSK-056→058, la evidencia más fuerte posible de cero cambio de
comportamiento.

**Cadena TSK-056→057→058 completa.** Verificado de punta a punta: `bun test` 222/222,
`tsc --noEmit` limpio en cada paso.
