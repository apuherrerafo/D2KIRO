---
id: TSK-106
title: "Fase 5: limpieza — retirar Basic Auth y el KV de settings, env vars finales"
state: backlog
moscow: must
attempts: 0
preferred_tool: claude-code
assigned_tool: codex
simplicity_exception: true
created_at: 2026-08-24
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 5, `docs/specs/SPEC.md` §12.10, §12.17 (ticket
13, el último de la fase — "cierre"). Depende de **todos** los tickets anteriores de Fase 5
(TSK-094 a TSK-105) — es deliberadamente el último y va **solo**, sin mezclarse con ningún cambio
de comportamiento, mismo criterio que ya usó el proyecto en TSK-047 (Fase 3): si algo se rompe acá,
es inequívocamente la limpieza, nunca una duda sobre si el comportamiento nuevo tiene un bug.

En este punto de la fase, ya nada llama a las funciones/rutas que este ticket borra — es código
muerto, no una decisión de diseño nueva.

## Alcance — 3 archivos

### 1. `apps/web/proxy.ts` (o el archivo que quede tras TSK-102, si `isValidBasicAuth` sigue ahí
sin usarse)
Borrar `isValidBasicAuth` y cualquier referencia residual a `SITE_ACCESS_USER`/
`SITE_ACCESS_PASSWORD` que TSK-102 haya dejado de *usar* pero no de *tener*.

### 2. `apps/engine/src/db/queries.ts`
Borrar `getAllSettings`/`upsertSetting` — sin llamadores desde TSK-098 (que retiró `GET`/`PUT
/api/settings`). Si `settings` como tabla queda sin ningún otro uso real en el proyecto, no se
borra la tabla en este ticket (fuera de alcance — borrar una tabla es una decisión de esquema
aparte, no "limpieza de código muerto").

### 3. `.env.example`
Retirar `SITE_ACCESS_USER`/`SITE_ACCESS_PASSWORD`. Agregar `SESSION_SECRET`,
`INTERNAL_AUTH_SECRET`, `PUBLIC_BASE_URL` con comentarios de qué son y dónde se usan (mismo
formato que el resto del archivo, ej. `CAPTURE_TOKEN`).

## Casos a probar
1. `grep -r "isValidBasicAuth\|SITE_ACCESS_" apps/` no encuentra ninguna referencia sobreviviente
   fuera de comentarios históricos/`journal.md` (que no se tocan).
2. `grep -rn "getAllSettings\|upsertSetting" apps/engine/src/` solo encuentra la definición borrada
   (es decir, cero resultados) — o, si la decisión al ejecutar fue no borrarlas todavía por algún
   motivo real encontrado en el camino, documentarlo explícitamente en el ticket, nunca dejarlas a
   medio quitar en silencio.
3. `.env.example` tiene las 3 variables nuevas y no tiene las 2 retiradas.

## Fuera de alcance
- Borrar la tabla `settings` del schema — decisión de esquema aparte, no de este ticket.
- Cualquier cambio de comportamiento — si al ejecutar este ticket se nota que algo todavía depende
  de lo que se está por borrar, **se para y se corrige el ticket anterior correspondiente**, nunca
  se fuerza el borrado.

## Aceptación
1. `bunx tsc --noEmit` y `bun test` limpios en `apps/engine` y `apps/web` — cero regresión, este
   ticket no cambia ningún comportamiento observable.
2. Los 3 casos de arriba.
3. Criterio 1 de SPEC.md §12.14 confirmado de punta a punta con este ticket cerrado: Basic Auth
   retirado por completo, el login de Steam es el único gate de acceso al sitio.
4. `bash scripts/verify-simplicity.sh` PASS.
