---
id: TSK-041
title: "Deploy E -- .env.example (nunca existió)"
state: doing
moscow: must
attempts: 0
preferred_tool: codex
assigned_tool: codex
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: decisión de arquitectura de deploy (`docs/agents/journal.md`
evt-20260801-018). Es lo que faltó y detuvo el primer intento de `/castoff`
(`docs/agents/journal.md` evt-20260801-017) -- nunca existió ningún `.env.example` en el repo.
**Ejecutar después de TSK-037/038/039 (revisión de Codex)**: la aceptación exige que el archivo
liste exactamente las variables que el código real usa -- si se escribe antes, listaría variables
todavía no consumidas por ningún código, contradiciendo su propio criterio de aceptación. Va antes
de TSK-040 (el Dockerfile referencia estas mismas variables).

## Cambios exactos
Un `.env.example` en la raíz del repo (o uno por app, `apps/engine/.env.example` +
`apps/web/.env.example`, seguir la convención que ya usa el monorepo para configs por proceso),
listando **solo los nombres, nunca valores reales**:
- `ENGINE_PORT` (ya existe, opcional, default 4000).
- `CAPTURE_TOKEN` (ya existe -- documentar que en Railway conviene fijarlo como variable fija en
  vez de dejar que se genere en runtime, para no tener un valor distinto en cada redeploy).
- `ENGINE_DB_PATH` (ya existe).
- `ENGINE_INTERNAL_URL` (nueva, TSK-037).
- `DRAFT_LIVE_ENABLED` (nueva, TSK-038).
- `SITE_ACCESS_USER` / `SITE_ACCESS_PASSWORD` (nuevas, TSK-039).
- `NEXT_PUBLIC_ENGINE_WS_URL` (ya existe, opcional, default `ws://127.0.0.1:4000/ws/draft` --
  `apps/web/features/draft/DraftView.tsx`. Faltaba en esta lista original -- agregada tras verificar
  con `grep process\.env\.` contra todo el código real antes de dispatchear el ticket).

## Fuera de alcance
- Cualquier valor real -- solo nombres y un comentario de una línea explicando cada uno.
- Configurar las variables reales en el dashboard de Railway -- eso es parte del proceso de
  deploy en sí (`/castoff`), no de este ticket.

## Aceptación
- `.env.example` existe y lista exactamente las variables que el código real lee (verificable con
  un grep de `process.env.` contra el archivo -- ninguna variable usada en código debe faltar en
  el ejemplo, ninguna del ejemplo debe estar sin usar en código).
