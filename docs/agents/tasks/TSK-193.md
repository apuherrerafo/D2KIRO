---
id: TSK-193
title: "Fase 9.0-A · Estructura del harness (eval/ + data/ + docs/adr/) + 4 ADRs iniciales"
state: backlog
moscow: must
attempts: 0
preferred_tool: claude-code
assigned_tool: null
created_at: 2026-08-29
write_scope: ["eval/**", "data/**", "docs/adr/**", ".gitignore"]
implements: ["R3-13", "R3-5", "R3-4", "R3-2", "C4"]
---

## Contexto

Primer ticket del bloque A (harness). SPEC.md §15.4.1 y §15.4.7. Monta el esqueleto de carpetas y
los 4 ADRs iniciales — todo lo demás de Fase 9 escribe dentro de esta estructura.

## Alcance

1. **Carpetas** (con `.gitkeep` donde haga falta):
   - `eval/{golden,scenarios,baselines,reports}/`
   - `data/{curated,generated,schemas,metadata}/`
   - `docs/adr/`
2. **`.gitignore`**: ignorar `eval/reports/**` (salida de cada corrida), **versionar**
   `eval/baselines/**` (el número congelado). `data/generated/**` se versiona (es reproducible y
   auditable).
3. **Los 4 ADRs** (`docs/adr/ADR-NNN-<slug>.md`, formato: Contexto / Decisión / Alternativas
   consideradas / Consecuencias / Estado). Contenido ya decidido en SPEC — este ticket sólo lo
   redacta:
   - **ADR-001** — Jerarquía de autoridad L0 Contratos Duros > L1 ADR > L2 `architecture.md` >
     L3 `SPEC.md` > L4 código > L5 investigación > L6 memoria de agente (R3-4).
   - **ADR-002** — El pick profesional observado **no es ground truth**; no existe snapshot de meta
     point-in-time → el backtest es comparativo, nunca predictivo (SPEC §15.1 C4).
   - **ADR-003** — Frontera `data/curated/` (revisado por humano) vs `data/generated/` (salida de
     script); lo generado nunca pisa lo curado (R3-13).
   - **ADR-004** — Compromiso a percentiles empíricos como mecanismo de calibración, con **cutover
     diferido al gate de 9.1** (decisión D2 del `/pre-flight`).
4. **`data/schemas/` y `data/metadata/`**: dejar un `README.md` que documente el formato de
   procedencia obligatorio de §15.4.1 (`source`, `generatedAt`, `generatorVersion`, `sampleWindow`,
   `patch`, `rowCount`, `schemaVersion`).

## Criterios de aceptación

1. Las 3 jerarquías de carpeta existen y están en git (`git status` las muestra).
2. Los 4 ADRs existen, con las 5 secciones, `Estado: aceptado`.
3. `.gitignore` ignora `eval/reports/` y **no** ignora `eval/baselines/` ni `data/generated/`.
4. `bash scripts/verify-simplicity.sh` verde. `bunx tsc --noEmit` sin cambios (no hay `.ts` nuevo).

## Fuera de alcance

- **No se mueven** los 3 JSON curados de `apps/engine/src/signals/` y `draft-paths/` a
  `data/curated/` — ticket propio, posterior al gate de 9.0 (SPEC §15.4.1, §15.10).
- Los hooks (TSK-194), los agentes (TSK-195), el split de `CLAUDE.md` (TSK-196).

## Gates

`verify-simplicity.sh`. `@redteam` no aplica (sólo estructura + docs, sin código, sin frontera).
