---
id: TSK-148
title: "Fase 7 G1: contratos de tipo + esquema de pro-drafts.sqlite"
state: doing
moscow: must
attempts: 0
preferred_tool: claude-code
assigned_tool: codex
created_at: 2026-08-27
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 7 (`SPEC.md` §14.4), **Gate 1, ticket 3**.
Depende de `TSK-147`. Es el contrato del que cuelga toda la fase — **ningún dato todavía**.

Decisión de arquitectura ya tomada por el usuario: los drafts crudos viven en una SQLite **aparte
y solo-dev** (`apps/engine/data/pro-drafts.sqlite`, al `.gitignore`, **nunca al volumen de
Railway**), y un paso de compilación offline produce un artefacto JSON versionado en el repo que es
**lo único que lee el motor**. Precedente directo: `capabilities.json` y `hero-positions.json`.

## Alcance — 2 archivos

**Nuevo** `apps/engine/src/pro/types.ts`:

```ts
export type Confidence = "high" | "medium" | "exploratory" | "none";

export interface ProSourceRef {
  readonly source: "opendota_match" | "opendota_league" | "opendota_position_est";
  readonly fetchedAt: string;      // ISO
  readonly sampleSize: number;
}

export interface ProTournament {
  readonly leagueId: number;
  readonly name: string;
  readonly tier: "premium" | "professional" | "excluded" | "amateur" | "unknown";
  readonly firstSeenAt: string;    // derivado de /proMatches — /leagues no tiene fechas
  readonly lastSeenAt: string;
  readonly region: "unknown";      // literal: region viene null en LAN, verificado
  readonly ref: ProSourceRef;
  readonly confidence: Confidence;
}

export interface ProDraftTurn {
  readonly order: number;          // 0..23, tal cual viene de picks_bans
  readonly isPick: boolean;
  readonly heroId: HeroId;
  readonly team: 0 | 1;
}

export interface ProDraftSlot {
  readonly heroId: HeroId;
  readonly team: 0 | 1;
  readonly positionEst: 1 | 2 | 3 | 4 | 5;
  readonly laneRole: number;
  readonly isRoaming: boolean;
  readonly netWorth: number;
}

export interface ProDraft {
  readonly matchId: string;
  readonly leagueId: number;
  readonly patch: string;
  readonly startTime: number;
  readonly gameMode: number;
  readonly radiantTeamId: number | null;
  readonly direTeamId: number | null;
  readonly winningSide: "radiant" | "dire";
  readonly turns: readonly ProDraftTurn[];
  readonly slots: readonly ProDraftSlot[];
  readonly ref: ProSourceRef;
}
```

**Nuevo** `scripts/pro/schema.sql` — el esquema de la base solo-dev: `tournaments`, `pro_drafts`,
`pro_draft_turns`, `pro_draft_slots`, `ingest_checkpoint`. **SQL plano con `bun:sqlite`, NO Drizzle**
— Drizzle es exclusivo de la base de producción (`engine.md`), y esta base nunca se despliega.

## Reglas duras del contrato
- **`Confidence` es una unión cerrada de 4 miembros, no 3.** `"exploratory"` existe desde el
  contrato, no se agrega después: obliga a que todo consumidor la maneje explícitamente y hace
  imposible que un `default` la trague por accidente (`TSK-154`).
- **`source` y `confidence` son obligatorios, nunca opcionales.** Un patrón sin procedencia **no
  compila**. Es el mismo mecanismo que hizo obligatorio `accountId` en `buildMetaSnapshot`
  (Fase 5): que rompa la compilación es la funcionalidad, no un defecto a suavizar.
- **`region` es el literal `"unknown"`, no un `string`.** Verificado: `region` viene `null` en LAN.
  Tipar el hueco impide que alguien lo rellene adivinando desde el nombre de la liga.
- `positionEst` es la estimación de OpenDota, **no verdad de campo** — su confianza se mide en
  `TSK-153`, no se asume aquí.

## Criterios de aceptación
- `bunx tsc --noEmit` limpio. Una prueba de tipos verifica que omitir `source` o `confidence` en un
  literal **no compila** (`@ts-expect-error`).
- `scripts/pro/schema.sql` se aplica sobre una base vacía y vuelve a aplicarse sin error
  (idempotente vía `CREATE TABLE IF NOT EXISTS`).
- `apps/engine/data/pro-drafts.sqlite` añadido al `.gitignore` en este mismo ticket.
- **Ningún archivo fuera de `apps/engine/src/pro/` y `scripts/pro/` cambia.**

## Fuera de alcance
Poblar nada. `ProPatternIndex` (el contrato del artefacto compilado) se define aquí solo si sale
gratis; si arrastra decisiones de agregación, se difiere a `TSK-155`, que es su dueño real.
