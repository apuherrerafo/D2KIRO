---
id: TSK-146
title: "Fase 7 G1: higiene — árbol de git, tablero, memoria y tsc limpio"
state: done
moscow: must
attempts: 0
preferred_tool: claude-code
assigned_tool: claude-code
created_at: 2026-08-27
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 7 (`docs/specs/SPEC.md` §14), **Gate 1, ticket 1**.
Sin dependencias — es el primero de la fase y desbloquea todo lo demás.

Antes de agregar 3.000 drafts profesionales al repo hay que dejarlo en un estado verificable. Hoy
no lo está, y ninguna de las tres cosas de abajo tiene ticket propio (auditoría 2026-08-27).

## Alcance — tres bloques independientes, un solo ticket

**1. Árbol de git.** Nueve tickets (`TSK-126`→`132`, `144`, `145`) y el código de `TSK-144`/`TSK-145`
(`signals/relationship-index.ts`, `signals/synergy-index.ts` y sus pruebas) están **sin trackear**.
Otros ~40 archivos están modificados sin commitear. Organizar en commits atómicos por ticket, mismo
criterio que ya se usó en `evt-20260824-109` (82 archivos → 14 commits).

**2. Deriva de tipos.** `bunx tsc --noEmit` en `apps/engine` falla en **9 archivos preexistentes**
por una deriva de `DraftState`: `TSK-072` le agregó `firstPickSide`, `turnStartedAt` y
`reserveRemainingMs`, y varias fixtures de prueba nunca se actualizaron. `TSK-089` arregló solo la
suya. Sin esto, **ningún type-check de la Fase 7 es señal fiable** — es la razón real por la que
este bloque va primero.

**3. Tablero y memoria.**
- `docs/agents/PROGRESS.md`: el bloque "SIGUIENTE PASO" apunta a `TSK-126`, cerrado el 2026-08-26.
- `docs/agents/journal.md`: sin entradas desde `TSK-133`. Añadir las que faltan (`TSK-133`→`145`)
  con el formato fijo de `CLAUDE.md` (`event:` + `schema:v1`). **Append-only — nunca se borra una
  línea existente**, `verify-simplicity.sh` bloquea cualquier diff que las elimine.
- `docs/agents/MEMORY.md`: contiene "Los 67 tickets del repo (TSK-001 a TSK-067) están done.
  Backlog en cero", resto de una regeneración vieja y hoy falso (son 144, ahora 161 con Fase 7).

## Criterios de aceptación
- `bunx tsc --noEmit` **limpio** en `apps/engine` y en `apps/web`. Es el criterio duro del ticket.
- `git status` sin archivos sin trackear que pertenezcan a un ticket ya cerrado.
- `bash scripts/verify-simplicity.sh` pasa.
- `bun scripts/hub.ts` regenera el tablero y muestra los 17 tickets de Fase 7 en `backlog`.
- Ninguna línea existente de `journal.md` ni de `ledger.md` desaparece del diff.

## Fuera de alcance — se anotan, no se arreglan aquí
- `team_synergy` devolviendo `raw: 0` en vez de `null` sin capacidades (viola `engine.md`; hallazgo
  de Fase 4.1, §13.17-5). Ticket propio.
- `hero-line-profiles.json` con 15/126 héroes (§13.17-6).
- `fingerprint()` sin `targetPosition`/`usePersonalPool` (§13.17-3).
- El hueco `TSK-143`: **no se rellena ni se reutiliza**. Se documenta como hueco huérfano
  permanente en `SPEC.md` §14.0.

## Cierre (2026-08-27)

Los 3 bloques se resolvieron, más 2 hallazgos que el alcance original no anticipaba:

1. **Árbol de git**: los 2 últimos commits locales (`388a070`, `c7adbec`, nunca pusheados) estaban
   **rotos** — `c7adbec` commiteó `ban-relief.ts` importando `./meta-matchup`, nunca commiteado.
   Corregido con `git reset --soft origin/master` (seguro, sin historia publicada de por medio) y
   reconstrucción en 15 commits atómicos por ticket, cada uno verificado con `tsc --noEmit` antes
   de avanzar al siguiente.
2. **Deriva de tipos**: **no existía.** `tsc --noEmit` ya pasaba limpio en `apps/engine` y
   `apps/web` — el hallazgo del audit previo a este ticket estaba desactualizado. Verificado, no
   asumido.
3. **Tablero y memoria**: `PROGRESS.md` (SIGUIENTE PASO actualizado), `MEMORY.md` (línea "67
   tickets, backlog en cero" corregida), `journal.md` (4 entradas nuevas, `TSK-133`→`146`, cero
   líneas existentes tocadas), `hub.html` regenerado.
4. **Hallazgo real, no anticipado, con decisión del usuario**: el árbol de trabajo traía
   `playerPosition`+`heroPool`+un `poolBonus` cableados en `app.ts` vía `requireHttpAccount`,
   haciendo que `/api/v1/draft/pro-recommendations` computara con el `accountId` real de la cuenta
   logueada — viola textualmente `SPEC.md` §12/§13 P14. Sin ticket, sin test de integración.
   Presentado al usuario, decidió revertir conservando `targetPosition`/`topN:5` (legítimos de
   TSK-133). Revertido antes de commitear. Consecuencia sin resolver a propósito: el selector de
   posición del simulador (`apps/web`, con tests) sigue enviando `playerPosition`, que el servidor
   ya no lee — queda como UI inerte, ticket aparte pendiente de crear.
5. **Hallazgo aparte, corregido de paso**: `index.ts` nunca pasaba `internalAuthSecret` a
   `createApp()` — el motor nunca activaba `multi_tenant` en producción pese a que Fase 5 está
   completa desde hace días. Verificado que no reabre el hallazgo 4.

`bun test`: 543/543 (engine) + 190/190 (web). `tsc --noEmit`: limpio en ambos paquetes.
`verify-simplicity.sh`: PASS. Detalle completo: `journal.md` evt-20260827-002.
