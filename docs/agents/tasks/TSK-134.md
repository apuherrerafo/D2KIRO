---
id: TSK-134
title: "Fase 6: espejos de apps/web/features/pro-drafter/types.ts"
state: done
assigned_tool: codex
moscow: must
attempts: 0
preferred_tool: codex
created_at: 2026-08-26
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 6, `docs/specs/SPEC.md` §13.10/§13.16-D y
§13.18 (ticket 9 de Bloque C). **Debe entrar en el mismo PR que TSK-133** — chico y autocontenido,
pero si va suelto el espejo queda desincronizado, exactamente el fallo que originó este ticket.

## Alcance — 1 archivo

**`apps/web/features/pro-drafter/types.ts`**:

```ts
export interface ProSuggestion {
  hero: HeroId;
  rank: 1 | 2 | 3 | 4 | 5; // antes: 1 | 2 | 3
  score: number;
  signals: ProSignalContribution[];
}

export interface LegacySuggestionSetResponse {
  schema: "suggestions/v1";
  suggestions: { hero: HeroId; rank: 1 | 2 | 3 | 4 | 5; score: number }[]; // antes: 1 | 2 | 3
}
```

**Hallazgo real, no hipotético (SPEC §13.16-D)**: `LegacySuggestionSetResponse` ya está mal **hoy**,
antes de esta fase — con `ENABLE_PRO_DRAFTER` apagado (el default) y `teamOpening: true`, `app.ts`
responde en esa misma URL con el `SuggestionSet` de v5, que ya trae ranks 4 y 5. Es una mentira de
tipo preexistente que este ticket corrige de paso.

**Nada más de `apps/web` cambia.** Sin pantallas nuevas, sin `ProDrafterPanel` reescrito, sin tocar
`bot-drafter.ts`. `ProDrafterPanel` renderiza la lista que recibe — 5 elementos en vez de 3 no
exigen ningún cambio de componente.

## Criterios de aceptación

- `bunx tsc --noEmit` limpio en `apps/web` tras el cambio.
- Ningún componente de `apps/web` necesita tocarse — si `tsc` señala un error en algún consumidor
  de estos tipos, es una señal de que el ticket creció fuera de su alcance declarado; investigar
  antes de silenciarlo.

## Fuera de alcance
Cualquier cambio visual o de componente en `apps/web`.
