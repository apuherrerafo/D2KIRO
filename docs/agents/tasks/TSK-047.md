---
id: TSK-047
title: "Fase 3: verificación final de baja de role-gap.ts / role-safety.ts (reducido, ver nota)"
state: done
moscow: must
attempts: 0
preferred_tool: claude-code
assigned_tool: null
---

## Nota de ejecución — alcance reducido por hallazgo real en TSK-045
El plan original era borrar estos 4 archivos **al final**, después de TSK-045 y TSK-046, para no
mezclar "el motor cambia de comportamiento" con "se limpia código muerto" en el mismo diff. Ese
plan asumía que los archivos podían quedar **huérfanos pero vivos** (compilando, sin uso) entre
medio. Eso resultó falso: en cuanto TSK-045 quita `"role_gap"`/`"role_safety"` de `SignalId`
(types.ts), esos archivos dejan de compilar de inmediato — no hay estado intermedio "huérfano y
sano" que sostener hasta este ticket. Se borraron ya, dentro de TSK-045, documentado en su propia
nota de ejecución y en `journal.md`. **El borrado en sí ya está hecho.**

Lo único que sigue teniendo sentido ejecutar acá, después de TSK-046 (que corrige el espejo de
`SignalId` en `apps/web`), es la verificación de que no quedó ninguna referencia viva en código.

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 3, `docs/specs/SPEC.md` §10.11 punto 5.
Depende de **TSK-046** (para que `apps/web` también esté libre de referencias antes de verificar).

## Alcance (reducido)
- ~~Borrar los 4 archivos~~ — ya hecho en TSK-045.
- Solo verificación (ver abajo) + cierre del ticket.

## Verificación previa obligatoria (antes de borrar nada)
```bash
grep -rn "roleGapScorer\|roleSafetyScorer\|role-gap\|role-safety\|role_gap\|role_safety" apps/ docs/ .claude/
```
Lo único que puede seguir apareciendo son **menciones históricas en documentación**
(`journal.md`, `SPEC.md` §9/§10, `PROGRESS.md`, tickets viejos, secciones de `.claude/rules/`
que explican la fusión). Esas **se conservan** — son registro de por qué el proyecto es como es,
y `journal.md` es append-only por regla dura.

Si aparece cualquier referencia **en código** de `apps/`, este ticket se detiene: significa que
TSK-045 o TSK-046 quedaron incompletos.

## Fuera de alcance
- Tocar cualquier otra señal.
- Reescribir historia en `journal.md` — prohibido por regla dura del proyecto
  (`verify-simplicity.sh` bloquea cualquier diff que elimine líneas de ahí).

## Aceptación
- Los 4 archivos borrados.
- `grep` de arriba sin ninguna coincidencia en código de `apps/`.
- `bun test` completo en ambos paquetes sin regresiones (el conteo total de pruebas baja — es
  esperado, son las pruebas de las dos señales dadas de baja).
- `bunx tsc --noEmit` y `bun run lint` limpios en ambos paquetes.
- `SCORING_WEIGHTS_V1/V2/V3` **siguen intactas en `weights.ts`** — se congelan por nombre, no se
  borran nunca, aunque referencien señales que ya no existen como scorer. Es el patrón declarado
  del proyecto desde 1b.
