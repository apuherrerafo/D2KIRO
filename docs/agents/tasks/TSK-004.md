---
id: TSK-004
title: applyDraftEvent puro + contrato de eventos (S1, S4)
state: done
moscow: must
attempts: 1
preferred_tool: codex
assigned_tool: claude-code
---

## Estado — 2026-07-27 (cerrado tras @shipcheck)
✅ Implementado en `apps/engine/src/draft/reducer.ts` + `reducer.test.ts`. @redteam: APPROVED
(gate seguridad PASS, 1 ronda). verify-simplicity.sh: 369 líneas / 2 archivos — supera las 200,
acordado explícitamente con el usuario antes de empezar (ver `journal.md` evt-20260727-014 en
adelante), mismo patrón que TSK-003.

**Nota abierta para tickets futuros (S1 ingest / candidatos de C3):** `unknown_hero` aquí solo
valida forma (entero positivo) — el reductor es puro y no tiene acceso a la tabla `heroes` de
SQLite (C4). El comentario de `SPEC.md` línea 178 ("el HeroId no existe en la tabla local de
héroes") describe la semántica final del sistema, no de este ticket en aislamiento. La validación
real contra el catálogo de héroes queda pendiente de dónde se decida que viva — candidato natural:
el borde de ingesta (S1, futuro TSK del servidor Bun) o el paso 1 (CANDIDATOS) del pipeline de C3.

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Este ticket implementa el reductor de estado del draft — el corazón de
C2. Es una función **pura**: sin I/O, sin `Date.now()`/generador de ids propio (se inyectan como
parámetros si hacen falta). No depende de TSK-001/002/003 más allá de compartir el mismo
proyecto `apps/engine`.

## Contrato de eventos (S1), versionado — implementar tal cual
```ts
type HeroId = number;
type TeamSide = 'radiant' | 'dire';
type DraftFormatId = 'all_pick' | 'captains_mode';
type CaptureSource = 'simulator' | 'manual' | 'overwolf' | 'ocr';

interface DraftEventEnvelope {
  schema: 'draft-event/v1';
  eventId: string;
  sessionId: string;
  seq: number;
  emittedAt: string;
  source: CaptureSource;
  confidence: number;
  payload: DraftEvent;
}

type DraftEvent =
  | { type: 'session_started'; format: DraftFormatId | 'unknown'; patch: string }
  | { type: 'local_side_identified'; side: TeamSide }
  | { type: 'hero_banned';  hero: HeroId; side: TeamSide | 'unknown' }
  | { type: 'hero_picked';  hero: HeroId; side: TeamSide }
  | { type: 'pick_reverted'; hero: HeroId; side: TeamSide }
  | { type: 'session_ended'; reason: 'completed' | 'aborted' | 'lost_capture' }
  | { type: 'capture_health'; status: 'ok' | 'degraded' | 'lost'; detail?: string };
```

## Estado (C2) e interfaz pura (S4) — implementar tal cual
```ts
interface DraftState {
  sessionId: string;
  schema: 'draft-state/v1';
  format: DraftFormatId | 'unknown';
  patch: string;
  localSide: TeamSide | 'unknown';
  phase: 'idle' | 'active' | 'complete' | 'aborted';
  banned: HeroId[];
  picks: { radiant: HeroId[]; dire: HeroId[] };
  lastSeq: number;
  appliedEventIds: string[];
  quality: { unconfirmed: HeroId[]; captureStatus: 'ok' | 'degraded' | 'lost' };
  updatedAt: string;
}

function applyDraftEvent(
  state: DraftState,
  envelope: DraftEventEnvelope,
): { state: DraftState; rejected?: RejectionReason };

type RejectionReason =
  | 'duplicate_event' | 'stale_seq' | 'wrong_phase'
  | 'unknown_hero' | 'hero_already_taken';
```

## Reglas de error, explícitas y obligatorias
- Transiciones: `idle → active` (por `session_started`), `active → complete`
  (`session_ended: completed`), `active → aborted` (`session_ended: aborted|lost_capture`). Una
  nueva `sessionId` vuelve a `idle`.
- `eventId` repetido → se descarta en silencio (`duplicate_event`).
- `seq <= lastSeq` → se rechaza (`stale_seq`), **salvo** `pick_reverted`, que siempre se evalúa.
- Un evento rechazado nunca lanza excepción ni corrompe el estado — se devuelve el
  `RejectionReason`, el estado anterior sigue siendo válido.
- `format: 'unknown'` es un estado legítimo — no se bloquea nada por no conocer el formato.
- **No se modela la tabla de turnos de Valve** (el algoritmo exacto de bans de All Pick 7.35d no
  está documentado con claridad) — nunca adivines un turnero. El orden de turnos vive como datos
  (`DraftFormat`, tabla por modo), no como lógica en el reductor.
- `pick_reverted` deshace un pick/ban reportado antes por error (falso positivo de captura) —
  debe poder aplicarse aunque su `seq` sea menor al `lastSeq`.

## Aceptación
- Función pura verificable: misma entrada siempre produce la misma salida, cero efectos
  secundarios.
- Pruebas cubren cada `RejectionReason` con un caso concreto.
- Pruebas cubren las 4 transiciones de fase y confirman que eventos en fase `complete` se
  rechazan con `wrong_phase`.
