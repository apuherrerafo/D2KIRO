---
id: TSK-040
title: "Deploy D -- Dockerfile + railway.json + volumen para la SQLite"
state: done
moscow: must
attempts: 0
preferred_tool: codex
assigned_tool: codex
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: decisión de arquitectura de deploy (`docs/agents/journal.md`
evt-20260801-018). **Orden de ejecución acordado tras revisión de Codex: 037 → 038 → 039 → 041 →
040 (este, último)** -- puede prepararse en paralelo, pero validar el contenedor de verdad
necesita que env vars, auth, rewrites y el guard de `/draft` ya existan.

**Por qué un Dockerfile y no autodetección de Railway (Railpack/Nixpacks)**: hay dos árboles de
dependencias con lockfiles distintos (`apps/engine/bun.lock` y `apps/web/package-lock.json`) y dos
procesos que tienen que arrancar juntos en el mismo contenedor -- la autodetección no expresa eso
de forma confiable.

## Diseño
- **Un `Dockerfile` en la raíz del repo**, no buildpack.
- `scripts/start-railway.sh` (nuevo): en este orden exacto --
  1. Corre la migración de Drizzle (`bun run db:migrate` en `apps/engine`) -- **nunca en el
     build**, los volúmenes de Railway no se montan en build time ni en pre-deploy, solo en
     runtime.
  2. Arranca `apps/engine` en background.
  3. Espera a que `127.0.0.1:4000/api/health` responda antes de seguir (nunca asumir que ya
     arrancó).
  4. Arranca `apps/web` (`next start -H :: -p $PORT`) -- escuchar en `::`/`$PORT` es correcto acá,
     la regla de `127.0.0.1` es sobre `apps/engine`, `apps/web` **debe** ser el servicio público.
- `railway.json`: healthcheck apuntando a `GET /healthz` (TSK-039) -- confirma que el engine
  local responde, no solo que Next está arriba.
- **Volumen**: `apps/engine/src/db/client.ts` hoy usa
  `process.env.ENGINE_DB_PATH ?? "./data/dota2coach.sqlite"` (path relativo, dependiente del
  `WORKDIR` del contenedor). Decisión cerrada (revisión de Codex): fijar
  `ENGINE_DB_PATH=/data/dota2coach.sqlite` (path absoluto) y montar el volumen de Railway en
  `/data` -- así no depende de dónde Railway coloque el `WORKDIR`, y el mismo volumen cubre
  también los archivos `-wal`/`-shm` de SQLite (viven en el mismo directorio). Si la imagen corre
  como usuario no-root, puede hacer falta `RAILWAY_RUN_UID=0` para que el proceso pueda escribir
  ahí.
- Activar backups automáticos del volumen en la configuración de Railway (fuera del repo, en el
  dashboard -- documentarlo en el ticket, no es algo que el código pueda forzar).

## Decisión cerrada: npm/Node para `apps/web`, no migrar a Bun
(Revisión de Codex, menor riesgo real): `apps/web` ya tiene `package-lock.json`, Next 16, eslint y
todo el tooling probado sobre npm. Migrar a Bun cambia gestor de paquetes, lockfile, resolución de
dependencias, y potencialmente el comportamiento de Next -- superficie de riesgo innecesaria justo
en un ticket de deploy, donde el objetivo es confiabilidad, no pureza de toolchain. La imagen
instala **ambos runtimes** (Bun para `apps/engine`, Node/npm para `apps/web`) -- dos runtimes es
menos elegante, pero no toca nada que ya funciona.

## Fuera de alcance
- Cualquier cambio al esquema de la base de datos -- la migración ya existe, solo se ejecuta en
  un lugar nuevo (arranque del contenedor, no build).
- Multi-región, autoscaling, o cualquier configuración de infraestructura más allá de un servicio
  simple con volumen.

## Aceptación
- La imagen buildea limpio localmente (`docker build`) antes de subirla a Railway.
- El contenedor arranca, aplica la migración, y ambos procesos responden -- verificable con un
  `docker run` local apuntando a un volumen de prueba antes del primer deploy real.
- Reiniciar el contenedor (simulando un redeploy) no borra los datos -- el volumen persiste.

## Nota de verificación (Claude Code no tiene Docker en su entorno)
Claude Code revisa el código (Dockerfile, `start-railway.sh`, `railway.json`) por lectura --
sintaxis, orden de arranque, healthcheck, path del volumen. La ejecución real de `docker build` /
`docker run` la corre el usuario en su máquina, con los comandos exactos que se le entreguen al
cierre del ticket. Sin esa confirmación del usuario, el ticket NO se marca `done` -- mismo
estándar que el resto de tickets de esta sesión (nunca cerrar sobre un reporte sin verificación
real).

## Estado — cerrado tras @shipcheck
Implementado por Codex, revisado a fondo por Claude Code (bug real corregido: `exec` al final de
`start-railway.sh` descartaba el `trap` de limpieza, dejando al motor huérfano en un redeploy real
-- corregido y verificado empíricamente contra bash real). El usuario instaló Docker Desktop (no
estaba presente) y corrió los 3 criterios de aceptación en vivo en su máquina: build limpio
(25/25 pasos), `GET /healthz` en 200 con ambos procesos sanos, y el dato de prueba
(`deploy_smoke=persisted`) sobrevivió un `docker stop` + `docker run` completo -- confirma que el
volumen en `/data` persiste entre reinicios, el criterio más importante del ticket. `@redteam`:
APPROVED, gate de seguridad PASS. Con esto se cierran los 5 tickets del bloque de deploy
(TSK-037/038/039/040/041) -- queda reintentar `/castoff` desde el principio. Ver
`docs/agents/journal.md` evt-20260801-037/038/039.
