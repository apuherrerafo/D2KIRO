---
id: TSK-195
title: "Fase 9.0-A · Dos agentes nuevos: data-stat-engineer + evaluation-engineer (privilegio mínimo)"
state: done
moscow: must
attempts: 0
preferred_tool: claude-code
assigned_tool: claude-code
created_at: 2026-08-29
write_scope: [".claude/agents/data-stat-engineer.md", ".claude/agents/evaluation-engineer.md", "CLAUDE.md"]
blocked_by: ["TSK-193"]
implements: ["R3-6", "R3-14"]
---

## Contexto

SPEC.md §15.4.7. El roster actual (Warden/Artisan/Chronicle/Tracer/Sentinel) cubre 4 de los 6
roles del informe #3. Faltan los 2 que Fase 9 usa a diario.

## Alcance

1. **`.claude/agents/data-stat-engineer.md`** — `model: claude-sonnet-5`,
   `tools: Read, Glob, Grep, Bash, Write, Edit`. **Sin `mcp__context7`** (R3-14). Cuerpo: percentiles
   empíricos, Empirical Bayes, procedencia de datos. **Veto explícito de escritura** sobre
   `apps/engine/src/**`, `apps/web/**` y cualquier archivo de scoring. Alcance de escritura:
   `data/generated/**`, `data/metadata/**`, `scripts/stats/**`, `eval/**`.
2. **`.claude/agents/evaluation-engineer.md`** — mismos `model`/`tools`, sin `mcp__context7`.
   Cuerpo: backtest, Golden Dataset, métricas, `gate.ts`. Mismo veto de escritura. Alcance:
   `eval/**`, `scripts/eval/**`.
3. **`CLAUDE.md`**: agregar las 2 filas a la tabla de agentes (misma forma que Warden/Artisan/etc.),
   con la nota de que ninguno corre en Opus y ninguno recibe MCP.

## Criterios de aceptación

1. Los 2 `.md` existen con frontmatter válido (`name`, `description`, `model`, `tools`).
2. Ninguno lista `mcp__context7` ni `mcp__*` en `tools`.
3. El cuerpo de cada uno nombra explícitamente los directorios que **no** puede tocar.
4. `CLAUDE.md` tiene las 2 filas nuevas en la tabla de agentes. `verify-simplicity.sh` verde.

## Fuera de alcance

- Renombrar o tocar los 5 agentes existentes.
- Cablear estos agentes a `/dispatch` (se hace cuando haya tickets asignados a ellos).

## Gates

`verify-simplicity.sh`. `@redteam` recomendado (toca la tabla de privilegios de agentes).
