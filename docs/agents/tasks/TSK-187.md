---
id: TSK-187
title: "Fase 8B — higiene de superficie: NavBar de 7 links a 4 (reversible, no borra nada)"
state: done
moscow: should
attempts: 0
preferred_tool: codex
assigned_tool: claude-code
created_at: 2026-08-28
---

## Contexto

El usuario quiere el nav enfocado en el flujo real (login + cuenta + pool + Simulador de Draft) y
sacar de la vista lo que no se está usando ahora, **sin borrar rutas ni código** — reversible,
todo sigue alcanzable por URL. Contrato en `SPEC.md` §14.8. Reglas en `.claude/rules/web.md`
("Fase 8"). Bloque **independiente de 8A** — se puede hacer en cualquier orden.

Ticket autocontenido para Codex: es editar un array + una prueba, sin necesidad de memoria del
proyecto.

## Alcance

1. **`apps/web/components/nav-bar/NavBar.tsx`** — el array de links pasa de 7 a **4**:
   - `Simulador de Draft` → `/simulator`
   - `Mi pool` → `/hero-pool`
   - `Meta` → `/meta`
   - `Configuración` → `/settings`
   - **Se quitan del array** (no del repo): `Draft en vivo` (`/live-draft`), `Equipos`
     (`/team-groups`), `Héroes` (`/heroes`).
2. La prop `draftLiveEnabled` de `NavBar` queda sin uso → retirarla de la firma y de los
   llamadores (más limpio) **o** dejarla (ambas válidas — decisión de quien implementa; si se
   retira, ajustar los sitios que la pasan).
3. **No se toca** ninguna ruta, `page.tsx`, componente ni test de `/live-draft`, `/team-groups`,
   `/heroes`. Siguen resolviendo por URL directa. `/live-draft` ya renderiza
   `DraftUnavailablePage` con `DRAFT_LIVE_ENABLED` apagado (default) — cero cambio de
   comportamiento. Redirects legacy (`/draft` → `/live-draft`, `/random-draft` → `/simulator`)
   quedan.
4. **Prueba** (`NavBar.test.tsx` o equivalente): `NavBar` renderiza exactamente 4 links con esos
   `href`. Humo: una prueba (o confirmación de que las suites existentes siguen verdes) de que
   `/team-groups`, `/heroes`, `/live-draft` siguen montando por URL directa.

## Criterios de aceptación

1. `bunx tsc --noEmit` limpio en `apps/web`. `bun test` verde **sin que ninguna prueba existente
   cambie de resultado** (8B no cambia comportamiento, sólo visibilidad).
2. `NavBar` renderiza 4 links (`/simulator`, `/hero-pool`, `/meta`, `/settings`).
3. `/team-groups`, `/heroes`, `/live-draft` siguen resolviendo por URL directa; sus suites siguen
   verdes.
4. Si se retiró `draftLiveEnabled`: no queda ningún llamador pasándola; si se dejó: no rompe nada.
5. `bash scripts/verify-simplicity.sh` pasa.

## Fuera de alcance

Borrar rutas, `page.tsx` o componentes. Tocar `proxy.ts` / el gate de sesión de Steam (sacar
links del nav no cambia qué rutas existen ni el perímetro de auth). Overwolf / OCR: ya dormidos
(nunca construidos / spike sin correr), quedan en stand-by — no se toca `scripts/spikes/`.
Cualquier cosa de 8A.

## Gates

`bunx tsc --noEmit` + `bun test` en `apps/web`, `bash scripts/verify-simplicity.sh`. Trazabilidad
en `journal.md`. Sin `@redteam` obligatorio (sin frontera nueva, sin cambio de comportamiento) —
opcional a criterio del revisor.
