---
id: TSK-131
title: "Fase 6: run-pipeline.ts — 7º parámetro, pesos por fase y fuente de matchups"
state: done
moscow: must
attempts: 0
preferred_tool: codex
created_at: 2026-08-26
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 6, `docs/specs/SPEC.md` §13.4, §13.5, §13.8 y
§13.18 (ticket 6 de Bloque B). Depende de TSK-127 (`phase-decay.ts`) y TSK-128
(`meta-matchup.ts`). **Todavía sin el modo `teamOpening` completo** — eso es TSK-132, que depende
de este.

## Alcance — 2 archivos

**`apps/engine/src/pipeline/run-pipeline.ts`**:

1. Nuevo 7º parámetro `options?: ProDrafterPipelineOptions`:
   ```ts
   export interface ProDrafterPipelineOptions {
     readonly teamOpening?: boolean; // sin usar todavía en este ticket
     readonly matchups?: Record<HeroId, HeroMatchupStat[]>;
     readonly heroCapabilities?: readonly HeroCapabilities[]; // sin usar todavía en este ticket
   }
   ```
2. **Selección de fuente de matchups**: si `options.matchups` está presente, usar
   `createMetaMatchupWinrate(options.matchups)` (TSK-128); si no, `corpusMatchupWinrate` **sin
   cambios**. `corpusMatchupWinrate` no se borra — `scripts/evaluate-pro-drafter.ts` depende de su
   metodología leave-one-out restringida al corpus.
3. **Pesos por fase**: reemplazar el uso directo de `weights` (el parámetro estático) por
   `deriveContinuousPipelineWeights(weights, ownPickCount, enemyPickCount)` (TSK-127) antes de
   pasarlo a `mergePipelineSignals`.

`PipelineCandidateResult` no cambia de forma. `PipelineSignalId` no gana una cuarta clave.

## Impacto conocido y obligatorio en pruebas existentes

`run-pipeline.test.ts` usa `STATE = picks { radiant: [1], dire: [10, 11] }` → `own=1`, `enemy=2`,
`t=0.25`. Los números trazados a mano de ese archivo **cambian** con los nuevos pesos por fase y
**deben actualizarse en este mismo ticket** — no es una regresión, es el cambio de comportamiento
que `SPEC.md` §13.4 especifica explícitamente.

## Criterios de aceptación (SPEC §13.14, criterio 6)

- Con `teamOpening` ausente: sigue devolviendo 3 resultados, mismas señales.
- Con `own+enemy >= 4`: `deriveContinuousPipelineWeights` devuelve `base` idéntico —
  comportamiento de fase media/tardía sin cambios.
- Con `own+enemy < 4` (incluido el fixture existente de `run-pipeline.test.ts`): los números
  esperados se actualizan en este ticket, con la tabla exacta de `SPEC.md` §13.4 como referencia.
- Con `options.matchups` inyectado: el pipeline usa `createMetaMatchupWinrate`, verificable con un
  caso donde el resultado difiere del que daría `corpusMatchupWinrate` sobre el mismo estado.

## Fuera de alcance
El modo `teamOpening` completo (guarda de apertura, KNN saltado, `denial_score` ban-aware,
desempate, diversificación) — eso es TSK-132.
