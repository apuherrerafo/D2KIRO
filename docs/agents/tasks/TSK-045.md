---
id: TSK-045
title: "Fase 3: SCORING_WEIGHTS_V4 + integración en mix.ts + candado de regresión"
state: done
moscow: must
attempts: 0
preferred_tool: claude-code
assigned_tool: claude-code
simplicity_exception: true
---

## Nota de excepción de simplicidad (agregada 2026-08-22, al armar el commit)
Toda la cadena TSK-043→047 se ejecutó en una sola sesión sin commits intermedios (instrucción
explícita del usuario en su momento: "vamos flecha hasta terminar"). Al commitear recién ahora,
el diff real de "Fase 3 completa" son ~20 archivos (motor + espejo en apps/web + rulebook +
blueprint) -- se commitea como una sola unidad lógica coherente (mismo criterio que el catch-up
de TSK-016 documentado en `journal.md`), referenciando este ticket por ser el que integra el
cambio de comportamiento real del motor.

## Nota de ejecución — desviación de alcance documentada
El ticket decía "Borrar `role-gap.ts`/`role-safety.ts` y sus pruebas (TSK-047) — quedan huérfanos
pero vivos" (Fuera de alcance, arriba). Eso resultó ser **imposible**: en cuanto `SignalId`
(types.ts) deja de incluir `"role_gap"`/`"role_safety"`, esos dos archivos y sus tests dejan de
**compilar** — no quedan huérfanos-pero-vivos, quedan huérfanos-y-rotos, porque su propio `id:
"role_gap"` ya no es un `SignalId` válido. El criterio de aceptación 6 de este mismo ticket
("`bunx tsc --noEmit` limpio en `apps/engine`") es, por lo tanto, incompatible con dejarlos vivos.
Decisión: se borraron acá, adelantando el núcleo de TSK-047 a este ticket. TSK-047 queda reducido
a lo que sí es opcional después de esto (si queda algo — revisar antes de ejecutarlo). Documentado
en `journal.md`.

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 3, `docs/specs/SPEC.md` §10.3 y §10.9.
Depende de **TSK-044**. Este es el ticket que hace que la fase **sirva de algo**: sin él,
`position_fit` existe y calcula bien, pero no cambia lo que el motor sugiere.

Contexto que importa: el bug original **no era que `role_gap` calculara mal**. Era que pesaba
`0.108` contra `0.288` de `counter` — detectaba el doble carry y perdía igual. Repetir ese error
con mejor dato sería no arreglar nada.

## Alcance

### 1. `apps/engine/src/signals/weights.ts`
```ts
export const SCORING_WEIGHTS_V4: Record<SignalId, number> = {
  position_fit: 0.25,
  counter: 0.27,
  patch_meta: 0.17,
  team_synergy: 0.14,
  hero_pool_fit: 0.17,
};
```
Suma exacta `1.0`. **V1/V2/V3 quedan intactas y congeladas por nombre** — no se editan, no se
borran, mismo patrón que ya siguen entre sí. Sus tipos `SignalIdV1`/`SignalIdV2` seguirán
excluyendo señales; ajustar esas exclusiones para que sigan compilando tras el cambio de la unión
`SignalId`, **sin cambiar ningún valor numérico de V1/V2/V3**.

**Por qué 0.25 y no 0.208** (la suma de las dos señales que reemplaza): ver arriba. 0.25 la deja
por encima de `patch_meta`/`hero_pool_fit` y apenas debajo de `counter`, coherente con la
jerarquía real de decisión de un draft (posición > contrarresto > sinergia).

### 2. `apps/engine/src/signals/types.ts`
`SignalId`: quita `"role_gap"` y `"role_safety"`, agrega `"position_fit"`.

### 3. `apps/engine/src/signals/mix.ts`
- `SCORERS`: saca `roleGapScorer` y `roleSafetyScorer`, mete el `position_fit` construido con la
  fábrica de TSK-044.
- `RAW_RANGE`: saca las dos entradas viejas, agrega `position_fit: [0, 1]`.
- Cambia `SCORING_WEIGHTS_V3` → `SCORING_WEIGHTS_V4` en las 3 referencias de este archivo.
- `BuildSuggestionsOptions` gana `heroPositions?: HeroPositions` — ausente carga el archivo real
  (`loadHeroPositions()`). Mismo patrón que `now?`/`metaIsStale?` ya usan ahí, así **ningún
  llamador existente cambia** (`app.ts` incluido).

## Fuera de alcance
- `apps/web` (TSK-046).
- Borrar `role-gap.ts`/`role-safety.ts` y sus pruebas (TSK-047) — quedan huérfanos pero vivos.

## Aceptación
1. Prueba unitaria: los 5 pesos de V4 suman exactamente `1.0` (mismo candado que V1/V2/V3).
2. V1, V2 y V3 siguen existiendo con **sus valores numéricos originales sin tocar**, y sus
   pruebas existentes siguen pasando sin modificarse.
3. **Candado de regresión del bug original** (el criterio central de toda la fase): contra
   `buildSuggestions` **completo** —no la señal aislada— con Spectre pickeado del lado propio y
   Wraith King disponible, **Wraith King no aparece en el top 3**. Probar contra el pipeline
   real es el punto: la señal aislada puede dar el número correcto y el ranking seguir mal si el
   peso no alcanza, que es exactamente lo que pasaba antes.
4. `buildSuggestions` sin `heroPositions` en las opciones sigue funcionando (carga el archivo
   real); con `heroPositions` inyectado usa el fixture (S10).
5. `bun test` completo en `apps/engine` sin regresiones. **Nota esperada**: las pruebas de
   `mix.test.ts` que hoy verifican el candado de regresión cero de V2/V3 van a necesitar ajuste
   —ese candado **no se hereda** en V4 (§10.0 punto 4: V4 reemplaza dos señales por una, no hay
   estado "sin configurar" que reproducir)—. Ajustarlas conscientemente y documentar por qué en
   el propio test, **nunca borrarlas en silencio**.
6. `bunx tsc --noEmit` limpio en `apps/engine`. (En `apps/web` va a fallar hasta TSK-046 — es
   esperado y es precisamente lo que prueba que el espejo de tipos existe.)
