---
id: TSK-161
title: "Fase 7 G3: re-corrida del gate de Fase 6 + paquete de decisión"
state: done
assigned_tool: codex
moscow: must
attempts: 0
preferred_tool: claude-code
created_at: 2026-08-27
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 7 (`SPEC.md` §14.4-I), **Gate 3, ticket 3 —
cierra el Gate 3 y decide si el Gate 4 llega a existir**. Depende de `TSK-160`.

**La Fase 6 está entera y apagada.** `TSK-126`→`TSK-142` están `done`, y `ENABLE_PRO_DRAFTER` sigue
en `false` porque el gate no pasa. Números registrados (2026-08-26):

| Métrica | Medido | Barra |
|---|---|---|
| Jaccard top-5 entre variantes de ban | **0.480** | ≤ 0.35 |
| Cambio de rank 1 | **28%** | ≥ 60% |
| Estabilidad ante bans irrelevantes (Role-Pressure) | **27,3%** | ≥ 80% |
| Cambio ante bans pivotales | 94,9% | ≥ 50% ✅ |

La causa declarada en `PROGRESS.md` y en `TSK-136`/`TSK-137` es **volumen de datos**: solo
**1.200 de 15.984** filas de `hero_matchups` superan el umbral de 200 partidas (§13.16-G), y la
corrida de Explorer sumó ~102 pares. Toda la Fase 7 existe para atacar esa causa raíz.

## Alcance — una corrida y un documento

1. Re-correr `bun scripts/evaluate-pro-drafter.ts --ban-sensitivity` sobre el corpus ampliado.
2. Escribir el paquete de evidencia en `docs/research/`.

## Regla dura, la que define el ticket
**No se edita ni una sola constante.** Ni `BETA_OPENING`, ni `POSITION_OVERLAP_GAIN`, ni
`MIN_MATCHUP_GAMES`, ni `OPENING_REPEAT_STRATEGY_PENALTY`, ni los umbrales del gate, ni los pesos de
`pro-drafter-weights-v6.json`. **Si se editó una, el resultado no es válido y el ticket no está
hecho.**

El punto entero es medir si *el dato* mejoró el resultado. Tocar una perilla en la misma corrida
hace la medición inútil y reintroduce exactamente el problema que `TSK-136` ya documentó (subir
`BETA_OPENING` a 0.65 y 0.7 "empeoró el resultado y se descartó como sobreajuste").

## El paquete de decisión
Alimenta un **segundo `/blueprint`, más angosto**, que elige entre las tres opciones que la Fase 6
ya dejó planteadas (§13.15):

- **(a)** encender el flag **solo para el caso de apertura**;
- **(b)** portar únicamente el término ban-aware a `team-opener.ts` (que sigue en producción con
  `MAX_COUNTER_RELIEF = 0.12`, vivo por §13 P9);
- **(c)** seguir a oscuras y curar más dato.

**Este ticket no decide nada.** Produce la evidencia; la decisión es del `/blueprint`.

## Criterios de aceptación
- El informe compara los 4 números nuevos contra los 4 registrados arriba, lado a lado.
- Declara explícitamente si cada barra se superó, **una por una** — no un veredicto global.
- `git diff` sobre `apps/engine/src/pipeline/` y `scripts/role-pressure.ts` está **vacío** al cerrar
  el ticket. Es la verificación mecánica de que no se tocó una constante.
- Si el gate **no** pasa: se registra en `journal.md`, `ENABLE_PRO_DRAFTER` sigue apagado, y **el
  Gate 4 no arranca**. Es un resultado válido del ticket, no un fallo.
- Incluye la cobertura de héroes del corpus medida en `TSK-160` (§13.16-H).

## Fuera de alcance
Encender `ENABLE_PRO_DRAFTER` — ningún ticket de esta fase lo toca. Retirar `MAX_COUNTER_RELIEF`
(ticket posterior con su propio candado de regresión, §13.17-7). Ejecutar el segundo `/blueprint`.

## Cierre

La corrida reproducible quedó documentada en `docs/research/tsk-161-gate3-decision.md`. Mejoraron
Jaccard (0.278) y cambio de rank 1 (90 %), mientras la estabilidad ante bans irrelevantes sigue en
27.3 %. Al no superar esa barra, el Gate 3 no pasa, `ENABLE_PRO_DRAFTER` continúa apagado y el
Gate 4 queda pendiente de un segundo blueprint.
