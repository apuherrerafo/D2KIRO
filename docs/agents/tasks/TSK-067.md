---
id: TSK-067
title: "Calibración del ecosistema de agentes contra el stack real + hardening mecánico del gate"
state: done
moscow: should
attempts: 0
preferred_tool: kiro-nativo
assigned_tool: claude-code
created_at: 2026-08-22
simplicity_exception: true
exception_reason: "Auditoría de agente único (agent-auditor) sobre 14 archivos de contexto/gobernanza que deben quedar mutuamente consistentes entre sí (AGENTS.md, .kiro/steering/*, .claude/agents/sentinel.md, 3 SKILL.md, MEMORY.md/USER.md/CONTEXT.md, scripts/{verify-simplicity,sync-context}.ts) -- partirlo en piezas de 3 archivos dejaría el ecosistema en un estado intermedio inconsistente (ej. sentinel.md ya sin HTMX pero USER.md todavía sin el perfil que lo explica). Ninguno es código de producto (`apps/engine`/`apps/web`), cero riesgo de regresión funcional: `bun test` 240/240 sin cambios."
---

## Origen -- ticket de registro retroactivo, mismo patrón que TSK-065/066

Encontrado igual que TSK-066: un lote completo de archivos modificados/nuevos, sin commitear,
timestamps 15:54-15:57 de hoy (antes de que esta sesión empezara a trabajar) -- otra sesión
(Kiro, rol "agent-auditor") hizo esta auditoría en paralelo, nunca la journaleó ni commiteó.

## Alcance

**Ya hecho por la sesión de Kiro** (auditado archivo por archivo antes de dar por bueno,
no asumido):
- `AGENTS.md`: reemplaza la plantilla genérica (`[nombre-del-proyecto]`, `Frontend: HTMX`) por el
  stack real (Next.js/Bun/Drizzle/Railway) y las reglas de seguridad reales.
- `.claude/agents/sentinel.md`: reemplaza los vectores específicos de HTMX (que este proyecto
  nunca usó) por los invariantes reales de `.claude/rules/security.md` -- `0.0.0.0`, cero red en
  `signals/`, `x-capture-token` + rate limit, allowlist de CDN de Valve, `account_id` nunca
  logueado.
- `.claude/skills/redteam/SKILL.md`: reemplaza "daisyUI" (nunca usado, este proyecto usa
  shadcn/ui) por los tokens de diseño reales (`--surface-*`, `--content-*`, etc., `web.md`).
- `.claude/skills/castoff/SKILL.md`: documenta que `verify-simplicity.sh` ya corre
  `sync-context.ts` como primer paso.
- `.kiro/steering/{product,structure,tech}.md`: reflejan Fase 3 completa, `SCORING_WEIGHTS_V5`,
  y la auditoría de arquitectura posterior a Fase 3.
- `docs/agents/{USER,CONTEXT}.md`: creados -- perfil de usuario y glosario de dominio/stack.
- `docs/agents/MEMORY.md`: regenerado completo (última regeneración real: 2026-07-28, cubría
  67 líneas de `journal.md`; ahora cubre las 645+).
- `scripts/sync-context.ts` (nuevo): corre `bun scripts/hub.ts`, chequea que `AGENTS.md`/
  `.kiro/steering/tech.md` no mencionen stack obsoleto (`HTMX`, `daisyUI`, plantilla sin llenar) y
  sí el real (`Next.js`, `Bun`, `SQLite`, `Drizzle`); chequea que `.kiro/steering/{product,
  structure}.md` reflejen `position_fit` sin dejar `role_gap`/`role_safety` mencionadas como
  vigentes; chequea que `plan.md` no liste como backlog tickets que ya están `done` en su propio
  frontmatter, y que `MEMORY.md` mencione el ticket más reciente del repo. **Nunca bloquea** --
  `process.exit(0)` siempre, es señal de mantenimiento, no gate de seguridad/correctitud.
- `scripts/verify-simplicity.sh`: llama a `sync-context.ts` como primer paso (informativo); agrega
  3 invariantes absolutos de arquitectura, sin excepción posible (a diferencia del límite de
  archivos/líneas, que sí admite `simplicity_exception`): `0.0.0.0` bajo `apps/engine/src/`,
  `fetch(` bajo `apps/engine/src/signals/`, `dangerouslySetInnerHTML` bajo `apps/web/`.

## Correcciones hechas en esta sesión antes de dar el trabajo por bueno

1. **Contradicción real encontrada en 3 archivos** (`@build`/`SKILL.md`, `USER.md`, `MEMORY.md`):
   los tres decían "no preguntes por adelantado, @build ya se ajustó a este patrón" -- pero
   `MEMORY.md` (antes de su propia reescritura) decía literalmente lo opuesto: *"pregunta ANTES de
   empezar (`AskUserQuestion`) ... **nunca asumir la respuesta de antemano**"*. La sesión de Kiro
   leyó "la respuesta ha sido consistente" y concluyó "entonces no preguntes más" -- exactamente lo
   que esa misma regla prohíbe. Corregido en los 3 archivos: `@build` sigue avisando con
   `AskUserQuestion` en cuanto detecta que va a superar el límite (no se detiene a mitad de una
   unidad lógica para eso), nunca asume en silencio.
2. **Verificado, no asumido, que los 3 checks nuevos de `verify-simplicity.sh` (sección 7) no
   producen falsos positivos contra el código real actual** -- corrida directa de los 3 `grep`
   exactos contra `git ls-files` de `apps/engine/src/`/`apps/web/`: cero coincidencias. Diseño
   correcto: escaneo literal (`grep -F`) scopeado a directorios de código, nunca a `docs/`/reglas
   (donde estas mismas cadenas aparecen constantemente en prosa explicando la regla).
3. **`scripts/sync-context.ts` ejecutado de verdad** (no solo leído) -- funciona, encontró 2
   señales reales legítimas (`plan.md` mencionaba 13 tickets ya `done`; `.kiro/steering/
   structure.md` menciona `role_gap`/`role_safety` sin un marcador histórico que el regex
   reconozca cerca de esa mención específica -- posible ajuste futuro del heurístico, no urgente).

## Verificación

`bun test` en `apps/engine`: 240/240, sin cambios (nada de este ticket toca `apps/engine/src` ni
`apps/web` -- es documentación, agentes, skills y scripts de gobernanza). `verify-simplicity.sh`
corrido con `COMMIT_TICKET=TSK-067`: confirmar en la nota de ejecución del commit real.
