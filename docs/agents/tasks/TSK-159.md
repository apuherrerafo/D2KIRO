---
id: TSK-159
title: "Fase 7 G4: cablear pro_evidence al pipeline Pro-Drafter"
state: backlog
moscow: should
attempts: 0
preferred_tool: claude-code
created_at: 2026-08-27
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 7 (`SPEC.md` §14.4-F), **Gate 4, ticket 2**.
Depende de `TSK-158`. ⚠️ **Solo existe si el Gate 3 pasó.**

**Es el único ticket de toda la Fase 7 que toca código del pipeline en producción.** Máximo cuidado.

## Alcance — 4 archivos

- `apps/engine/src/pipeline/merge.ts` — `PipelineSignalId` gana una 4ª clave:
  `"knn_similarity" | "lane_score" | "denial_score" | "pro_evidence"`, más su entrada en
  `PIPELINE_RAW_RANGE`.
- `apps/engine/src/pipeline/run-pipeline.ts` — 5ª etapa opcional. Los modos `teamOpening` y normal
  **no cambian de forma**.
- `apps/engine/src/pipeline/weight-loader.ts` + **nuevo** `pro-drafter-weights-v7.json` — 4 pesos,
  suma `1.0` verificada con `SUM_EPSILON = 1e-9`. `v6.json` **no se borra ni se edita**: se congela
  por nombre, mismo patrón que `SCORING_WEIGHTS_V1`–`V5`.

## Por qué `PipelineSignalId` es el lugar correcto
Fase 6 dejó ese universo **separado a propósito** de `SignalId` (`.claude/rules/engine.md`, §Fase 6:
*"Toda dimensión nueva vive en el universo ya separado de `pipeline/merge.ts`"*). Ampliarlo aquí es
usar el escape que se dejó abierto, no forzar uno nuevo. `SignalId` sigue congelado en 5 miembros y
el motor v5 de producción no se entera de nada.

## Reglas duras
- **`mix.ts`, `weights.ts`, `SignalId` y `SCORING_WEIGHTS_V1`–`V5` no se tocan.**
- **`apps/web` no se toca.** Cero pantallas, cero espejos de tipo nuevos.
- **`ENABLE_PRO_DRAFTER` sigue apagado por defecto**, sin cambio de semántica.
- Sin `pro-patterns.json`, `pro_evidence` es `raw: null` y la redistribución proporcional de
  `mergePipelineSignals` reproduce **exactamente** el comportamiento de hoy.
- Se respeta el presupuesto: la etapa nueva no puede empujar el pipeline sobre los 200 ms
  (`PIPELINE_TIMEOUT_MS`).

## Criterios de aceptación
- **Candado de regresión cero, con números, no a ojo:** con `pro-patterns.json` ausente, el pipeline
  devuelve **exactamente** el mismo ranking y los mismos scores que antes de este ticket. Prueba
  dedicada contra el pipeline completo, no contra `merge.ts` aislado — mismo criterio que Fase 3
  (§10.9-7), Fase 5 (§12.14-2) y Fase 6 (§13.14-3).
- Los 4 pesos de `v7.json` suman `1.0` dentro de `1e-9`. Prueba unitaria obligatoria, como en toda
  versión de pesos del proyecto.
- `SignalId` sigue teniendo 5 miembros; `SCORING_WEIGHTS_V5` sigue sumando `1.0`.
- **Prueba dedicada:** ninguna fila `exploratory` mueve el score al pasar por el pipeline completo
  (la regla de `TSK-154`/`TSK-158` sobrevive a la integración — es donde más fácil se pierde).
- El modo normal (sin `teamOpening`) sigue devolviendo 3 resultados; el de apertura, 5.
- Prueba de rendimiento con margen generoso.
- Aislamiento de árboles intacto (criterio 13 de §13.14): ningún archivo de `pipeline/` importa
  `mix.ts`, `weights.ts` ni `SignalId`.

## Decisión pendiente de aprobación
**Decisión 8 (`SPEC.md` §14.10):** ampliar `PipelineSignalId` a una 4ª clave.

## Fuera de alcance
Encender el flag. Retirar `MAX_COUNTER_RELIEF`. Tocar `apps/web`. El spike de embeddings
(`TSK-162`).
