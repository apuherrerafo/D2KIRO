---
id: TSK-024
title: "Pantalla de configuración: Mi pool de héroes (RTK Query)"
state: done
moscow: must
attempts: 1
preferred_tool: codex
assigned_tool: claude-code
simplicity_exception: true
---

## Estado — 2026-07-29 (cerrado tras @shipcheck)
✅ Implementado en `apps/web/features/hero-pool/` (módulo nuevo) + `app/hero-pool/page.tsx` + 3
endpoints RTK Query en `engine-api.ts`. `@redteam`: **APPROVED** (gate de seguridad PASS, 1 ronda).
1 hallazgo real de correctness corregido en la misma ronda: mensajes de error genéricos para 400
(accountId inválido) y 409 (cálculo en curso), diferenciados por status code. `tsc --noEmit`
limpio, `eslint` limpio, `bun test` de apps/web sin regresión (23/23). `verify-simplicity.sh`: 6
archivos/238 líneas — excede ambos límites, excepción documentada por adelantado
(`simplicity_exception: true`), mismo patrón que TSK-012 en fase 1. **Pendiente**: verificación
visual en navegador real (sin herramienta de automatización disponible en esta sesión).

## Contexto (ticket autocontenido)
Proyecto: dota2coach — `apps/web` (Next.js App Router). Ya existen `app/settings/page.tsx`
(páginas de configuración vía RTK Query), `components/hero-icon/HeroIcon.tsx` (ícono oficial de
héroe, requisito duro) y `components/hero-picker/HeroPicker.tsx` (selector de héroe ya usado en la
entrada manual de draft — revisar si es reutilizable aquí antes de construir un selector nuevo).
Este ticket es la pantalla donde el usuario administra su hero pool a mano.

**Depende de TSK-020** (`GET`/`PUT /api/hero-pool` deben existir en `apps/engine`).
**No depende de TSK-021** (el botón "Calcular desde mis partidas" abre el flujo de TSK-025, que
puede construirse en paralelo — este ticket solo necesita el botón como punto de entrada, no la
pantalla de propuesta completa).

## Alcance — SOLO esto

- Nueva sección/página bajo configuración ("Mi pool de héroes"): decide si es una ruta nueva
  (`app/hero-pool/page.tsx`) o una sección dentro de `app/settings/page.tsx` — revisar cómo está
  organizada esa página antes de decidir, seguir el patrón que ya exista.
- Lista de hasta 5 héroes del pool actual (`GET /api/hero-pool` vía RTK Query), cada uno con su
  ícono oficial (`HeroIcon`, requisito duro de `web.md` — nunca un héroe sin ícono).
- Añadir un héroe a mano: selector (reutilizar `HeroPicker` si aplica), respetando el límite de 5
  desde la UI (deshabilitar "añadir" al llegar a 5, con el mensaje explicando por qué).
- Quitar un héroe del pool.
- Guardar (`PUT /api/hero-pool`) con los cambios acumulados — no un `PUT` por cada click de
  añadir/quitar, uno solo al confirmar (mismo espíritu que "nunca sobreescribe en silencio").
- Botón "Calcular desde mis partidas" que pide el `account_id` de Steam y abre el flujo de
  propuesta (TSK-025) — este ticket solo necesita el botón y el campo de `account_id`, la pantalla
  de propuesta en sí es TSK-025.
- Estado vacío ("aún no configuraste tu pool") explicado en llano, no una lista en blanco muda.

## Fuera de alcance — NO implementar en este ticket
- La pantalla de propuesta/confirmación tras "calcular" (TSK-025).
- Cualquier cambio a `apps/engine`.
- Hero pool de compañeros (fuera de alcance de toda la fase 1b, no solo de este ticket).

## Reglas duras
- Convenciones de `.claude/rules/web.md`: TypeScript estricto sin `any`, sin ternarios para
  renderizado condicional, sin funciones anónimas inline como manejadores, arquitectura por
  features, tokens de diseño semánticos (`--surface-*`, `--content-*`, `--accent-*`), nunca un hex
  suelto.
- RTK Query, nunca WebSocket/Zustand — esta pantalla es "página normal del sitio", no la vista de
  draft en vivo.
- Cada héroe se muestra siempre con su ícono oficial — requisito duro, sin excepción.
- Error boundary y estado de carga propios de esta feature (no uno genérico compartido).
- El pulido visual (hover/pressed/focus/disabled) es requisito duro para pantallas nuevas —
  `.claude/rules/web.md`.

## Aceptación
- Con el pool vacío, la pantalla explica en llano que no hay héroes configurados y ofrece las dos
  vías (añadir a mano / calcular).
- Añadir 5 héroes y guardar → `GET` posterior (recarga de página) refleja exactamente esos 5.
- Intentar añadir un sexto héroe está bloqueado desde la UI, con explicación visible.
- Quitar un héroe y guardar → desaparece de la lista tras recargar.
- Cada héroe en la lista muestra su ícono oficial, nunca un placeholder genérico.
