---
id: TSK-181
title: "Fase 4.3 — archetype_fit usable: selector de intención + transporte WS set_intent + validación de borde"
state: done
moscow: must
attempts: 0
preferred_tool: claude-code
assigned_tool: claude-code
simplicity_exception: true
created_at: 2026-08-28
---

## Contexto

4.2 (`TSK-180`) integró `archetype_fit` al motor como 6ª señal ponderada, pero **inerte**: sin
`archetypeIntent` la señal es `applicable: false` y no hay forma de elegir una intención desde la
app. 4.3 la hace **usable de punta a punta**. Contrato cerrado en `SPEC.md` §11.14 (`/blueprint`
corrido en Sonnet por decisión explícita del usuario). Reglas de fase en `.claude/rules/`
(secciones "Fase 4.3" en `engine.md`, `web.md`, `security.md`, `testing-seams.md`) y `CLAUDE.md`
("REGLAS DE FASE 4.3").

## Archivos (~10-12, `simplicity_exception: true`)

**Motor (`apps/engine/src/server/`):**
1. `session.ts` — `SessionEntry` gana `archetypeIntent: DraftPathArchetype | null` (default
   `null`); `SessionStore` gana `setArchetypeIntent(sessionId, intent)` / `archetypeIntent(sessionId)`;
   el merge de `applyDraftEvent` lo preserva (`?? null`) igual que `ownerAccountId`. `ClientMessage.type`
   gana `"set_intent"` + `archetypeIntent?: DraftPathArchetype | null`.
2. `edge.ts` — `isValidClientMessage` rama `set_intent` (`sessionId` no vacío **y**
   `archetypeIntent ∈ {4 literales, null}`); `SuggestionsPreviewRequest` gana `archetypeIntent?`;
   `isValidSuggestionsPreviewRequest` lo valida (presente ⇒ uno de los 4 literales, si no `400`).
3. `app.ts` — `computeSuggestionsForState` gana `archetypeIntent?` en `options`; los caminos en
   vivo (`hello`, push tras `/ingest/draft-event`, reconexión) leen
   `sessionStore.archetypeIntent(sessionId)` y lo pasan; `handleSuggestionsPreview` toma
   `body.archetypeIntent`; handler de `set_intent`: `setArchetypeIntent(...)` + si cambió,
   recalcula y publica **sólo `suggestions`** (no `snapshot`/`draft_state`), si no cambió no-op.
4. `edge.test.ts` — ramas nuevas de validación (criterios 4-5).
5. `app.test.ts` (o `session.test.ts`) — criterios 1-3, 6.

**`apps/web`:**
6. `features/draft/types.ts` — `type DraftArchetype` (espejo a mano de `draft-paths/types.ts`) +
   mirror de `ClientMessage` con `set_intent`.
7. `features/draft/store.ts` — `archetypeIntent` + `setArchetypeIntent(intent)` (set local +
   `socket.send({type:"set_intent", ...})`); `connect()` re-envía `set_intent` tras el `hello` si
   `archetypeIntent !== null`.
8. `features/draft/constants.tsx` — `ARCHETYPE_LABELS` (mismo vocabulario que las `explanation`
   del motor).
9. `components/draft-intent-selector/DraftIntentSelector.tsx` (nuevo) + `.test.ts` — 4 chips +
   "Sin intención"; handlers nombrados, sin anón inline, sin ternario de render; color por rol
   semántico + escala de 4 px + tipografía por rol.
10. `features/draft/DraftView.tsx` — monta `<DraftIntentSelector>` en `WaitingForDraftState`
    (`esperando_draft`) **y** en `ActiveDraftState`/`DegradedDraftState`.

## Criterios de aceptación (SPEC.md §11.14.9)

1. `bunx tsc --noEmit` limpio en `apps/engine` **y** `apps/web`. **Sin intención puesta, la vista
   en vivo devuelve exactamente las mismas `suggestions` que antes de 4.3** (candado de
   no-regresión en el camino WS).
2. `set_intent` con arquetipo válido → el motor empuja `suggestions` frescas donde `archetype_fit`
   vota y el top-3 se mueve. Contra `app.ts` completo (`SessionStore` + `buildSuggestions` reales),
   no la señal aislada.
3. `set_intent` con `archetypeIntent: null` → `archetype_fit` vuelve a `applicable: false`, el
   top-3 vuelve al orden sin intención.
4. `set_intent` malformado (`"carry"`, `123`, `{}`, sin `sessionId`) → mensaje descartado, la
   sesión no cambia, no lanza, no hay push. Prueba unitaria de `isValidClientMessage`.
5. `archetypeIntent` inválido en `SuggestionsPreviewRequest` → `400`. Prueba unitaria de
   `isValidSuggestionsPreviewRequest`.
6. Reconexión tras fijar la intención → sigue aplicada (server-side); el cliente re-envía
   `set_intent` tras el `hello` (prueba del store con `FakeSocket`).
7. `<DraftIntentSelector>` se renderiza en `esperando_draft` y en `activo`/`degradado`; elegir un
   chip llama `setArchetypeIntent` y manda el `set_intent` por el `FakeSocket`; el chip elegido
   muestra estado seleccionado; "Sin intención" limpia.
8. El selector usa color por rol semántico + escala de 4 px + tipografía por rol — ni un hex/px
   suelto (`web.md`, `@redteam` pasada 1).
9. La 6ª fila de `SignalBreakdown` (TSK-180) muestra valor y `explanation` reales una vez puesta
   la intención.
10. **QA manual (§11.14.8)** corrido y registrado en `journal.md`: draft vacío + los 4 arquetipos
    (el top-3 se inclina hacia la dimensión del arquetipo); con picks que ya cumplen la intención +
    un hard counter real → el counter / la posición faltante **sigue ganando** (`position_fit`
    manda); limpiar a mitad de draft; reconexión. Si el QA pide otro `w` → abrir el ticket de
    `SCORING_WEIGHTS_V7` (§11.14.7).

## Fuera de alcance

Tocar `signals/` o `SCORING_WEIGHTS_V6` (si el QA pide otro `w`, es un follow-up que acuña `V7`).
Decaimiento de la señal a lo largo del draft (§11.11, sigue abierto). Persistir la intención más
allá de la vida de la sesión en memoria. Que `apps/web` consuma `archetypeIntent` en
`/api/suggestions/preview` (el campo se agrega al contrato del motor; lo usa el bot/panel, no la
vista en vivo).

## Gates

`@redteam` obligatorio (nueva frontera de confianza + toca `server/`). `bunx tsc --noEmit` en
ambos paquetes, `bun test` verde en ambos, `bash scripts/verify-simplicity.sh`. Trazabilidad de
Q1-Q4 del blueprint y del QA manual en `journal.md`.
