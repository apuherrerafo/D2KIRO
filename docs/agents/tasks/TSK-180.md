---
id: TSK-180
title: "Fase 4.2 — integrar archetype_fit al motor (SCORING_WEIGHTS_V6, SignalId, mix.ts + espejo apps/web)"
state: done
moscow: must
attempts: 0
preferred_tool: claude-code
assigned_tool: claude-code
simplicity_exception: true
created_at: 2026-08-28
---

## Contexto

`archetype_fit` existe aislada desde 4.1 (`TSK-089`): señal pura con pruebas propias, **no
enchufada** al ranking. 4.2 es la integración — y con ella el único cambio de comportamiento real
de la Fase 4. Contrato cerrado en `SPEC.md` §11.13 (`/blueprint` corrido en Sonnet por decisión
explícita del usuario, 2026-08-28). Reglas de fase en `.claude/rules/` (secciones "Fase 4.2" en
`engine.md`, `web.md`, `security.md`, `testing-seams.md`) y `CLAUDE.md` ("REGLAS DE FASE 4.2").

**Alcance: sólo motor.** El selector de intención en `apps/web`, el transporte (campo en el
request de sugerencias y en el `hello` del WS) y la validación de borde de ese input son **4.3**,
no este ticket. `apps/web` en 4.2 se toca sólo para el espejo de tipos y para que `SignalBreakdown`
renderice la sexta fila.

## Archivos (~9-10, `simplicity_exception: true`)

Motor:
1. `apps/engine/src/signals/weights.ts` — re-tipar `SCORING_WEIGHTS_V4`/`V5` a
   `Record<SignalIdV5, number>` (literales históricos, valores intactos, mismo mecanismo que
   TSK-045). Añadir `SCORING_WEIGHTS_V6: Record<SignalId, number>`:
   `position_fit 0.342, counter 0.216, patch_meta 0.117, team_synergy 0.117, hero_pool_fit 0.108,
   archetype_fit 0.10` (los 5 heredados = valor de V5 × 0.90; Σ = 1.0). Congelar V6 por nombre.
2. `apps/engine/src/signals/types.ts` — `SignalId` += `"archetype_fit"` (6º literal).
3. `apps/engine/src/signals/archetype-fit.ts` — borrar `ArchetypeFitContribution` /
   `ArchetypeFitScorer`; `createArchetypeFitScorer` devuelve `SignalScorer`, `score()` devuelve
   `SignalContribution`. **Cuerpo, `ARCHETYPE_MAX_BONUS`, normalización, `explanation` y las 3
   ramas de resultado: sin cambios.**
4. `apps/engine/src/signals/mix.ts` — importar `SCORING_WEIGHTS_V6` (reemplaza V5 en
   `weightedContributions`/`buildReason`); `RAW_RANGE.archetype_fit = [0, 1]`;
   `BuildSuggestionsOptions.archetypeIntent?: DraftPathArchetype`; ensamblar
   `createArchetypeFitScorer(heroCapabilities, options.archetypeIntent)` por llamada junto a
   `position_fit`/`team_synergy`.
5. `apps/engine/src/signals/mix.test.ts` — candados (ver criterios 2-4).
6. `apps/engine/src/signals/archetype-fit.test.ts` — re-tipar a `SignalContribution`/`SignalScorer`
   (las 5 pruebas de §11.9 siguen pasando sin cambios de lógica).

Espejo `apps/web` (mismo PR, `web.md`):
7. `apps/web/features/draft/types.ts` — `SignalId` += `"archetype_fit"`.
8. `apps/web/features/draft/validation.ts` — `isSignalId` += `|| value === "archetype_fit"`.
9. `apps/web/features/draft/constants.tsx` — `SIGNAL_DISPLAY_PRIORITY` += `"archetype_fit"` al final.
10. `apps/web/components/signal-breakdown/SignalBreakdown.tsx` — `SIGNAL_LABELS` +=
    `archetype_fit: "Intención de draft"` (+ `SignalBreakdown.test.ts` si el conteo de filas está
    aseverado).

## Criterios de aceptación (SPEC.md §11.13.8)

1. `bunx tsc --noEmit` limpio en `apps/engine` **y** `apps/web`. `SCORING_WEIGHTS_V1..V5` sin un
   solo valor cambiado. Prueba de que los 6 pesos de V6 suman `1.0`.
2. **Candado de regresión cero numérico** (`mix.test.ts`): con `archetypeIntent` ausente,
   `mixScore(S)` sobre un set fijo `S` da **el mismo número exacto** con V6 que con V5
   (se importa `SCORING_WEIGHTS_V5` sólo para el test). No "no cambió a ojo".
3. **Candado de sensibilidad** contra `buildSuggestions` completo (no la señal aislada): con
   `archetypeIntent: "push"` y draft vacío, el top-3 se inclina hacia `structuralDamage` alto
   respecto del top-3 sin intención. **Prueba dedicada con `scaling` que invierte la inclinación.**
4. `position_fit` sigue siendo el mayor peso de V6 (`0.342` > todos) — aserción explícita.
5. Candidato sin entrada en las capacidades inyectadas + intención elegida → `raw: null` para
   `archetype_fit`, las otras 5 se calculan igual, **nunca una excepción sin capturar**.
6. `SignalBreakdown` renderiza 6 filas; sin intención la sexta usa la fila "no aplica"
   (`SignalBreakdownRowNotApplicable`) con el texto del motor, jamás "Sin datos suficientes".
7. Ninguna prueba lee `capabilities.json`/`hero-positions.json` real (S9/S10) — fixtures inline.
8. El espejo de `apps/web` se mueve en el mismo cambio: si el motor amplía `SignalId` y `apps/web`
   no, `tsc` de `apps/web` rompe. Es la funcionalidad, no un defecto a suavizar.

## Fuera de alcance (va a 4.3)

Selector de intención en la UI, transporte por request/`hello`, validación de borde de
`archetypeIntent` contra los 4 literales, decaimiento de la señal a lo largo del draft, ajuste
fino de `w = 0.10` tras el QA. Los 3 héroes sin `capabilities.json` (`131`/`145`/`155`) y el bug
`team_synergy` `raw: 0` → tickets propios, no bloquean 4.2.

## Gates

`@redteam` obligatorio (toca el motor). `bunx tsc --noEmit` en `apps/engine` y `apps/web`,
`bun test` verde en ambos, `bash scripts/verify-simplicity.sh`. Trazabilidad de las decisiones
Q1-Q5 del blueprint en `journal.md`.
