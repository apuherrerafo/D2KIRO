---
id: TSK-126
title: "Fase 6: mover openingStrategy a draft-paths/strategy.ts"
state: done
moscow: must
attempts: 0
preferred_tool: codex
created_at: 2026-08-26
---

## Contexto (ticket autocontenido)
Proyecto: dota2coach. Origen: `/blueprint` de Fase 6, `docs/specs/SPEC.md` §13.7 y §13.18 (ticket 1
de Bloque A). No depende de ningún ticket anterior de esta fase — va solo, mismo criterio que
TSK-047: si algo se rompe acá, es inequívocamente el movimiento, nada más.

Hoy `openingStrategy` vive como función privada dentro de `apps/engine/src/signals/mix.ts` (líneas
305-312 aprox.). El modo de apertura de Pro-Drafter (tickets siguientes de esta fase) necesita la
misma clasificación de arquetipo, y `signals/mix.ts` no puede importarse desde `pipeline/` (viola
el aislamiento de árboles que ya prueba `run-pipeline.test.ts`, criterio 13 de `SPEC.md` §13.14).

## Alcance — 2 archivos

1. **Nuevo** `apps/engine/src/draft-paths/strategy.ts`: una sola función, cuerpo copiado tal cual
   desde `mix.ts` (sin reescribir la lógica):
   ```ts
   import type { DraftPathArchetype, HeroCapabilities } from "./types";
   import type { HeroId } from "../draft/reducer";

   export function openingStrategy(hero: HeroId, capabilities: HeroCapabilities[]): DraftPathArchetype {
     const capability = capabilities.find((entry) => entry.hero === hero);
     if (!capability) return "scaling";
     if (capability.structuralDamage === "high") return "push";
     if (capability.teamfight === "high") return "teamfight";
     if (capability.hasInitiation && capability.hasCatch) return "pickoff";
     return "scaling";
   }
   ```
2. **`apps/engine/src/signals/mix.ts`**: borra la función privada, importa
   `openingStrategy` desde `../draft-paths/strategy`. El tipo de retorno pasa de la unión literal
   `"push"|"teamfight"|"pickoff"|"scaling"` a `DraftPathArchetype` — es la misma unión
   (`draft-paths/types.ts`), `mix.ts` compila sin ningún otro cambio.

## Criterios de aceptación (SPEC §13.14, criterio 10)

- `signals/mix.ts` no contiene ninguna función que clasifique arquetipos — solo el import.
- `bun test` de `mix.test.ts` y `team-opener.test.ts` pasa **sin cambiar ninguna expectativa** —
  es un movimiento mecánico, el comportamiento observable es idéntico byte a byte.
- Nueva prueba en un archivo propio (`draft-paths/strategy.test.ts`) que fija el comportamiento por
  cada rama (push/teamfight/pickoff/scaling/sin capacidades → scaling).
- **Una segunda copia de esta clasificación en cualquier otro archivo es rechazo automático de
  revisión** — verificar con `grep` antes de cerrar el ticket que no queda ninguna.

## Fuera de alcance
`extractCandidateStrategies` (pipeline/feature-extractor.ts, TSK-130) que consume esta función —
ese es el ticket 5 de esta fase, depende de este.
