---
description: Alias simple. Resuelve a /onboarding si el repo ya existe, o a /launchpad si es un proyecto nuevo.
---

Antes de responder: revisa si existe `docs/agents/PROGRESS.md` con contenido más allá de la plantilla inicial, y si el proyecto ya tiene código real (no solo el ecosistema instalado).

- Si el proyecto ya existe con código real: sigue las instrucciones de `.claude/skills/onboarding/SKILL.md`.
- Si es un proyecto nuevo, sin código todavía: sigue las instrucciones de `.claude/skills/launchpad/SKILL.md`.

En cualquier caso, lee el archivo completo antes de actuar.
